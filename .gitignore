.DS_Store

# Python cache/build artifacts
__pycache__/
*.py[cod]
*.so
*.dylib
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Packaging/build directories
build/
dist/
*.egg
pip-wheel-metadata/

# CMake build artifacts
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
compile_commands.json
Makefile

# Admin UI audit (local)
.duckclaw/

# Local runtime data and secrets
/data/
.env
.env.*
!.env.example
!.env.immutable
db/
*.duckdb
*.duckdb.wal
*.wal

# Security testing artifacts
strix_runs/
.security/

# Local PM2 / deployment config (secretos en .env; ecosystem *.config.cjs versionados en config/)
*.bak
config/api_gateways_pm2.json
config/dotenv_wizard_proposed.env
*.service

# LangGraph dev / Studio local state
.langgraph_api/

# Agent training conversation traces and local train artifacts
packages/agents/train/conversation_traces/
packages/agents/train/gemma4/
*.safetensors

# MkDocs build output (source of truth: docs/)
site/

# DuckDB extension cache (pytest)
.pytest_duckdb/

# MCP npm prefetch (scripts/prefetch_mcp_reddit.sh)
.mcp-cache/

# Runtime / debug logs
logs/
!apps/duckclaw-admin/src/app/api/admin/ops/logs/
!apps/duckclaw-admin/src/app/api/admin/ops/logs/stream/
!apps/duckclaw-admin/src/app/api/admin/ops/logs/stream/route.ts
# Local IDE / agent config (Cursor rules, skills, plans)
.agents/
.cursor/
debug_sft.log

# DuckOps migrate backups (local pre-migrate snapshots)
.duckops/migrate-backups/

# Deprecated local tests (require .env/broker)
tests/deprecated/

.cusorignore
.cursorrules

/db/
packages/agents/train/conversation_traces/
dump.rdb
# Productividad — artefactos locales del agente (no versionar contenido)
storage/artifacts/**
!storage/artifacts/.gitkeep

# Local generated output
output/

# External local bundles (not part of duckclaw monorepo)
/Leila/

# Local staging patches
_quant_reports_fix/
_auto_publish_fix/
_reports_fix_staging/
_reports_staging/
debug-*.log

# VPS-only: deploy patches, probes, host-specific ops (never commit)
_vps_local/