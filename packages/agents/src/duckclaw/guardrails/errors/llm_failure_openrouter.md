No pude completar la inferencia con OpenRouter. Comprueba la API key en Admin → Integraciones, créditos en openrouter.ai/settings/credits y que el modelo sea un slug OpenRouter (p. ej. `deepseek/deepseek-v4-flash`), no la API directa de DeepSeek.

Detalle: {detail}
