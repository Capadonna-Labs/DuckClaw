# DB-First Core Refactor

## Objetivo Del Estado Final

DuckClaw debe quedar como un core generico de orquestacion LangGraph/LangChain, con runtime DB-first y sin verticales hardcodeadas en Python. El core decide con datos: catalogo de workers, capabilities, runtime policies, prompt policies, proyectos, contextos, equipos, permisos y comandos tipados.

El estado objetivo no es "borrar todos los dominios del repo" de una vez. El objetivo es que el core no tenga conocimiento especial de dominios. Un dominio puede existir solo como configuracion DB-first creada por el usuario, como plantilla de catalogo, o como paquete/extension vertical fuera del core generico.

## Regla De Separacion Vertical

Estas verticales no pertenecen al core:

- Quant Trader.
- Finanz, Finance o IBKR.
- PQRSD o PQRS.
- Leila.
- War Room o WR.
- Job Hunter.

Regla obligatoria: esas verticales no deben aparecer como defaults, rutas especiales, prompts especiales, worker ids especiales, tablas especiales, env vars especiales o branches de decision dentro de `duckclaw.manager.*`, runtime gateway generico, bootstrap compartido, schema migrations core o comandos transversales. Solo pueden existir si el usuario las crea/configura via DB-first, o si viven como paquete/extension vertical fuera del core.

Si un futuro corte necesita comportamiento especifico de una vertical, debe modelarlo como capability, runtime policy, prompt policy, tool policy, plantilla de worker, grant o comando DB-first. No debe meter otro `if worker_id == "Quant-Trader"` ni otro alias de dominio en Python core.

## Decisiones Arquitectonicas Vigentes

- DuckDB es el source of truth del control plane: workers, capabilities, prompt policies, runtime settings, proyectos, contextos, knowledge sources, shared grants y team access.
- El indice vectorial y otros artefactos derivados son reconstruibles. La verdad operacional vive en tablas y comandos idempotentes.
- El gateway y los agentes deben operar en read-only salvo allowlists explicitamente justificadas. Las mutaciones deben pasar por DB-writer o por comandos tipados autorizados.
- Markdown runtime no se reintroduce como politica cargada por agentes. Markdown sigue permitido para specs y docs de ingenieria como este archivo.
- El manager es un orquestador generico. Routing, fast plans y clasificacion de tareas se basan en capacidades/policies, no en nombres de dominios.
- Los wrappers legacy pueden mantenerse solo como compatibilidad temporal si delegan en el owner nuevo y tienen tests que protejan la direccion del cambio.

## Lo Movido O Limpiado Hasta Ahora

### Manager Y Routing

- `duckclaw.manager.graph` es el owner de `build_manager_graph`; `duckclaw.graphs.manager_graph` queda como fachada de compatibilidad.
- `duckclaw.manager.routing` concentra helpers genericos de worker id, normalizacion y cache routing.
- `duckclaw.manager.task_classification` concentra clasificacion generica de contexto/memoria y superficie MCP ligera. Los tests prohiben frases verticales en este modulo.
- `duckclaw.manager.fast_plans` reemplaza fast plans hardcodeados por policies DB-first de capability `fast_plan`.
- `duckclaw.manager.fast_replies`, `duckclaw.manager.planning`, `duckclaw.manager.invocation`, `duckclaw.manager.worker_reply_formatting` extraen responsabilidades del grafo grande.

### Egress Y Evidencia

- `duckclaw.egress.evidence_validator` es el owner transversal de auditoria de evidencia/citas, incluyendo `bracket_citation_audit`.
- `duckclaw.egress.tool_response_repair` reemplaza el repair de respuestas de tool acoplado a mercado. Repara JSON crudo, respuestas vacias y ecos de tools sin mencionar verticales.
- `duckclaw.egress.user_reply_nl_synthesis` conserva sintesis NL de salida, pero los nuevos helpers transversales deben vivir fuera de verticales.
- `duckclaw.egress.market_worker_tool_repair` fue removido como owner canonico.
- `duckclaw.egress.job_hunter_output_validator` fue removido. La validacion laboral no pertenece al egress core; si Job Hunter vuelve, debe ser extension/config vertical fuera del core.

### Comandos Y DB-Writer

- `duckclaw.commands.chat_state` extrae estado chat-scoped antes mezclado en `on_the_fly_commands.py`. Tambien expone `set_chat_state_via_typed_command` para escribir claves `agent_config` con `UpsertAgentConfigEntriesCommand` cuando el handle esta en read-only, y `forget_chat_state_via_typed_command` para borrar historial `/forget` mediante `ForgetChatStateCommand`.
- `duckclaw.commands.team_templates` extrae equipo de chat/tenant y mantiene compatibilidad con imports legacy.
- `duckclaw.commands.team_access` extrae whitelist generica de Telegram Guard y usa comandos tipados para mutaciones de usuarios autorizados y shared grants.
- `duckclaw.commands.vaults` extrae `/vault` y sus helpers de sesion/gateway/template-bound vaults; `on_the_fly_commands.py` queda como fachada de compatibilidad y dispatcher.
- `duckclaw.commands.crons` extrae `/crons`, parseo/listado de intervalos `--delta`, horarios `--timestamp`, ids `--rm` y el mensaje SYSTEM_EVENT proactivo generico; `on_the_fly_commands.py` queda como fachada de compatibilidad y dispatcher.
- `duckclaw.commands.crons` ya no abre bóvedas hermanas con DuckDB read-write directo para limpiar schedules. Las limpiezas remotas de `agent_config` se encolan como `UpsertAgentConfigEntriesCommand` y las aplica el DB-writer/inline writer transaccional.
- `duckclaw.commands.goals` extrae `/goals`, helpers legacy de `agent_config` goals, listado/persistencia del manifiesto homeostasis y normalizacion de keys de señales; `on_the_fly_commands.py` queda como fachada de compatibilidad y dispatcher mediante callbacks explicitos para resolver LLM/vault user sin ciclos.
- `duckclaw.commands.goals` ya no resuelve el registry desde `workers/manifests`, `load_manifest`, `list_workers` ni carpetas `templates/workers`. El owner DB-first administrable es `duckclaw.worker_quality_signals`, expuesto como "señales de calidad" del agente y persistido en `admin_runtime_settings` con dominio `worker.quality_signals` por tenant/worker. Internamente alimenta `BeliefRegistry` para homeostasis, pero el lenguaje de producto no debe exponer "belief registry".
- `duckclaw.workers.manifest.WorkerSpec` ya no expone campos `*_config` por vendor/dominio (`github_config`, `quant_config`, `fmp_config`, `comfyui_config`, etc.). El contrato generico es `skill_configs: dict[str, dict]`, derivado del bloque `skills:`. La plantilla `default` declara integraciones como entradas de `skills`, no como claves top-level, y `workers/factory.py` lee configs con helper generico.
- `duckclaw.workers.skill_tool_registry` es el owner de registro lazy de tools por `skill_configs`. El registro usa descriptores declarativos `SkillToolRegistrar` (`skill_name`, `phase`, `registrar_path`, activacion por superficie/hint y contexto requerido) en vez de funciones `_register_<skill>` hardcodeadas. `workers/factory.py` ya no importa bridges concretos (`reddit_bridge`, `google_trends_bridge`, `openweather_bridge`, `comfyui_bridge`, etc.) ni contiene ramas directas por config vendor para registrar herramientas; solo invoca las fases genericas pre/post LLM del registry. Siguiente paso posible: persistir/administrar esos descriptores desde DB cuando el catálogo de skills soporte registrars.
- `duckclaw.workers.tool_invocation_policy` ya no importa `duckclaw.finance.runtime_policy` ni usa capabilities verticales (`finance_ledger`, `quant_trading`). Las decisiones forzadas usan capabilities transversales (`local_ledger`, `market_data_bridge`, `broker_market_data`) y razones genericas. `services/api-gateway` y `packages/shared` no deben contener schemas/strings verticales `quant_core`, `finanz`, `ibkr`, `fmp`, `crm` ni `sft_config`.
- `duckclaw.commands.sensors` extrae `/sensors`, helpers de diagnostico SSH/Tailscale, formato de lineas sensor y resumen de browser sandbox; `on_the_fly_commands.py` queda como fachada/dispatcher compatible y configura explicitamente el adaptador graph-local de sandbox para evitar ciclos.
- `duckclaw.commands.audit` extrae `/audit` y `save_last_audit` como estado chat-scoped de la ultima ejecucion; `on_the_fly_commands.py` queda como fachada/dispatcher compatible.
- `duckclaw.commands.history` extrae `/history`, `append_task_audit`, `get_history_limit_for_chat` y helpers de `task_audit_log`; `on_the_fly_commands.py` queda como fachada/dispatcher compatible. El append historico usa `AppendTaskAuditCommand` y `enqueue_typed_command` para handles read-only, evitando SQL crudo en la cola DB-writer.
- `duckclaw.commands.model_setup` extrae `/model`, `/models`, `/setup`, `/prompt`, resolucion efectiva de tripleta LLM y `get_effective_system_prompt`; `on_the_fly_commands.py` queda como fachada/dispatcher compatible. Los overrides chat-scoped `llm_*` viven en `admin_runtime_settings` via `duckclaw.runtime_session_settings` y los prompts de sistema viven en `main.prompt_policy_registry` via `UpsertPromptPolicyCommand`.
- `duckclaw.commands.health` extrae `/health` y `/heartbeat`; `on_the_fly_commands.py` queda como fachada/dispatcher compatible e inyecta explicitamente un adaptador graph-local hacia `chat_heartbeat` para evitar ciclos. `/health` solo lee `SELECT 1` y sondea inferencia. `/heartbeat` persiste el estado durable on/off en `admin_runtime_settings` via `duckclaw.heartbeat_runtime_settings` (`domain='runtime.heartbeat'`, `key='enabled'`, `actor_email='chat:<chat_id>'`) y usa `UpsertRuntimeSettingCommand`/DB-writer cuando el handle es read-only. Redis queda para Pub/Sub/SSE y fallback legacy de lectura/escritura solo cuando no existe handle DB.
- `duckclaw.commands.runtime_toggles` extrae `/sandbox`, `/internet` y aliases estrechos (`/sandox`, `/red`, `/network`); `on_the_fly_commands.py` queda como fachada/dispatcher compatible e inyecta explicitamente el cleanup graph-local de sesiones sandbox para evitar ciclos. Los toggles `sandbox_enabled` y `sandbox_network_enabled` viven en `admin_runtime_settings` con dominio `runtime.session` y se escriben con comandos tipados/DB-writer cuando el handle es read-only.
- `services/api-gateway/core/fly_command_invocation.py` ya no abre todos los slash/fly commands con `DuckClaw(read_only=False)`. El dispatcher mantiene `READ_ONLY_SAFE_FLY_COMMANDS` para comandos migrados y abre con `read_only=True`: `/context`, `/workers`, `/forget`, `/goals`, `/crons`, `/audit`, `/health`, `/heartbeat`, `/history`, `/sandbox`, `/sandox`, `/internet`, `/red`, `/network`, `/model`, `/models`, `/provider`, `/llm`, `/prompt`, `/meditate`, `/comfyui`, `/reject-code`, `/reject_code`, `/resolve-uncertainty`, `/resolve_uncertainty`, `/system_prompt`, `/system`, `/setup`, `/team` y `/vault`. `/context on|off` persiste `use_rag` mediante `UpsertAgentConfigEntriesCommand`; `/workers` persiste `team_templates` mediante `UpsertAgentConfigEntriesCommand`; `/forget` borra conversaciones/audit via `ForgetChatStateCommand`; `/crons` persiste schedules primarios mediante `UpsertAgentConfigEntriesCommand` cuando el handle es read-only; `/team` encola mutaciones de whitelist/grants con comandos tipados y libera el handle RO antes del DB-writer; `/vault` queda read-only-safe en gateway porque el fly handle trae `_path` y el comando solo reporta la bóveda fija de la sesión; `/meditate` persiste schedules via `UpsertAgentConfigEntriesCommand` cuando el handle es read-only.
- Estado actual de `LEGACY_RW_FLY_COMMANDS`: ninguno.
- `LEGACY_RW_FLY_COMMANDS` queda vacio en este corte y `_open_legacy_fly_duckclaw_rw` fue retirado. `_open_fly_duckclaw` abre siempre `DuckClaw(read_only=True)`; cualquier mutacion slash/fly debe pasar por comandos tipados y DB-writer.
- `duckclaw.graphs.tool_catalog` es el owner transversal de fallbacks seguros para graphs legacy: prompt minimo de `general_graph`, tools default read-only cuando no llega catalogo DB/caller, clasificacion SQL para heartbeat admin y mensajes genericos por tool. `general_graph.py` ya no carga prompt Markdown runtime ni habilita `admin_sql`/`run_sandbox` por omision.
- `packages/agents/src/duckclaw/graphs/on_the_fly_commands.py` ya no debe exponer alias `Trabajo -> Job Hunter`, mensajes de red sandbox que recomienden `Job-Hunter`, ni textos laborales como default de comandos transversales.
- `packages/agents/src/duckclaw/graphs/on_the_fly_commands.py` ya no conserva ramas, prompts ni comandos core de Quant/Finanz/Finance/IBKR/Trader. `/audit`, `/sensors`, `/crons`, `/goals` y `/vault` delegan en `duckclaw.commands.*`; `/lake` queda como diagnostico generico local que reutiliza el helper canonico de sensors. Comandos verticales de sesion, senales y broker ya no son propiedad del core command graph.
- `duckclaw.write_commands` define comandos Pydantic idempotentes para workers, worker capabilities, proyectos, runtime settings, team access, shared grants, Kanban, knowledge/RAG y prompt policies.
- `duckclaw.write_command_handlers` despacha esos comandos dentro de una transaccion administrada por el caller. Ya quedo como fachada/dispatcher estable: los handlers de runtime/chat-state/audit viven en `duckclaw.write_handlers.runtime`, los de team access/shared grants en `duckclaw.write_handlers.access`, los de admin auth/console users en `duckclaw.write_handlers.admin_auth`, los de workers/catalog/user agents/template contexts/capabilities en `duckclaw.write_handlers.workers`, los de workspace projects/managed draft en `duckclaw.write_handlers.workspace`, los de Kanban en `duckclaw.write_handlers.kanban`, los de prompt policies en `duckclaw.write_handlers.prompt_policies`, los de mantenimiento DuckDB en `duckclaw.write_handlers.duckdb_maintenance` y los de Knowledge/RAG en `duckclaw.write_handlers.knowledge`.
- `UpsertWorkerCapabilityCommand` registra/otorga capabilities DB-first a workers existentes por `worker_id` + `tenant_id`. La proteccion `bounded_select_star_read` de `read_pool` debe asignarse con este comando o con el catalogo admin a los workers que realmente deban bloquear `SELECT *` sin `LIMIT`; no se siembra como default vertical ni se hardcodea en Python.
- `services/api-gateway/routers/admin_domains/access_management.py` ya no escribe access mutators directamente; console users, Telegram whitelist y shared grants delegan en comandos tipados via DB-writer.
- `services/db-writer/main.py` ya no carga un loop de `quant_state_delta`; mantiene loops transversales/contextuales como context injection, visual, meditate y reports.
- `duckclaw.forge.team_env.default_tenant_id_from_env` ya no infiere tenants desde nombres PM2 ni rutas DuckDB y queda como compatibilidad env-only.
- El owner DB-first canonico del tenant default administrado es `main.admin_runtime_settings` con `tenant_id='global'`, `actor_email=''`, `domain='gateway'`, `key='default_tenant_id'`. Gateway, Telegram inbound, Telegram compact routes legacy y `/vault` resuelven con `duckclaw.forge.team_env.default_tenant_id_from_runtime`: env explicito administrado (`DUCKCLAW_GATEWAY_TENANT_ID` / `DUCKCLAW_TELEGRAM_DEFAULT_TENANT`) → lookup read-only del setting global → fallback seguro `default`. La configuracion debe persistirse via runtime settings/comando tipado; no por escritura directa desde gateway ni por heuristicas de proceso/path.

### Heartbeat

- `services/heartbeat/main.py` ya no resuelve sesiones ni bóvedas Quant-Trader, no consulta ni escribe `quant_core.*`, no extrae PnL desde respuestas, no fuerza `worker_id=finanz` en homeostasis y no permite ticks proactivos sin goals por ramas de trading. Conserva scheduler/homeostasis/proactive messaging como flujo transversal basado en `agent_config`, goals, worker_id/tenant_id y mensajes genericos hacia el gateway.
- `tests/test_forge_legacy_cleanup.py::test_heartbeat_base_has_no_quant_finance_trading_residue` protege que heartbeat base no reintroduzca marcadores Quant/Trader/Finance/IBKR/broker/trading.

### Homeostasis Y Goals Alignment

- `duckclaw.homeostasis.goals_alignment` es el owner transversal de alineacion de goals. Ya no lee estado de sesion, broker, schemas verticales ni bridges de dominio para derivar observaciones. La alineacion compara `observed_value`, `target_value`, `threshold` y `comparison` provenientes del manifiesto/registry DB-first.
- `duckclaw.homeostasis.surprise`, `duckclaw.homeostasis.belief_registry` y `duckclaw.homeostasis.manager` son los owners transversales de calculo de sorpresa, registry de creencias y action planning homeostasis.
- `duckclaw.worker_quality_signals` es el owner DB-first de las señales administrables de calidad por worker/tenant (`latency_ms`, `completion_rate_pct`, `error_rate_pct`, etc.). Usa `admin_runtime_settings` y traduce esas señales a beliefs solo como detalle interno de homeostasis.
- `duckclaw.forge.homeostasis.goals_alignment`, `duckclaw.forge.homeostasis.surprise`, `duckclaw.forge.homeostasis.belief_registry`, `duckclaw.forge.homeostasis.manager` y el package `duckclaw.forge.homeostasis` quedan como fachadas legacy temporales, sin logica propia, para callers que aun no deben tocarse en este corte.
- `duckclaw.db_write_queue` es ahora el owner canonico del singleton writer/cola DuckDB, incluyendo los adaptadores legacy `enqueue_write`, `execute_write_direct`, `WriteQueueBridge` y `run_consumer`. `duckclaw.forge.homeostasis.singleton_writer` queda como fachada legacy temporal sin logica propia.
- El nudging proactivo de desalineacion ya no agrega contexto Quant ni recomendaciones por objetivo vertical. Si una vertical necesita enriquecer observaciones, debe hacerlo como capability/policy DB-first o extension fuera del core antes de persistir `observed_value`.
- `tests/test_forge_legacy_cleanup.py::test_homeostasis_goals_alignment_has_no_quant_finance_trading_residue` protege que el owner canonico `duckclaw.homeostasis.goals_alignment` no reintroduzca marcadores Quant/Trader/Finance/IBKR/broker/trading.
- Los fixtures positivos de goals/homeostasis/meditate usan metricas genericas como `latency_ms`, `completion_rate_pct` y `error_rate_pct`; los ejemplos PnL/drawdown/trading quedan fuera del core o como guardrails negativos explicitos.
- `tests/test_forge_legacy_cleanup.py::test_on_the_fly_command_graph_has_no_quant_finance_trading_residue` ahora cubre tambien `pnl` y `drawdown` para evitar que copy/runtime transversal de `/goals` o `/crons` vuelva a usar ejemplos financieros.
- `docs/operations/Homeostasis-Heartbeat.md` y `Meditate-Homeostasis.md` usan ejemplos transversales de tenant/worker y metricas genericas; `tests/test_forge_legacy_cleanup.py::test_homeostasis_operation_docs_use_generic_metrics` evita reintroducir Finanz/Finance/PNL/drawdown/trading como ejemplos positivos.
- `tests/test_package_reorg_contracts.py::test_homeostasis_goals_alignment_implementation_is_owned_by_homeostasis_package` protege que los imports legacy deleguen al owner canonico y que `__module__` no vuelva a `duckclaw.forge.homeostasis`.
- `tests/test_package_reorg_contracts.py::test_homeostasis_runtime_implementations_are_owned_by_homeostasis_package` protege que `surprise`, `belief_registry` y `manager` deleguen desde Forge hacia `duckclaw.homeostasis`.
- `tests/test_forge_legacy_cleanup.py::test_canonical_homeostasis_package_does_not_depend_on_forge_homeostasis` protege que el paquete canonico `duckclaw.homeostasis` no importe de vuelta desde Forge.

### War Room

- `services/api-gateway/core/war_rooms.py` fue removido.
- `schema_migrations.py` y `scripts/bootstrap_dbs.py` ya no deben registrar ni crear `war_room_core`, `wr_members` ni `wr_audit_log`.
- `duckclaw.commands.team_access` no exporta comandos War Room. War Room solo puede volver como extension vertical externa o como configuracion DB-first creada por el usuario.

### Package Reorg Y Training

- Se retiraron paquetes legacy de Forge: `duckclaw.forge.industries`, `duckclaw.forge.crm`, `duckclaw.forge.quotes`, `duckclaw.forge.sft`, `duckclaw.forge.models` y `duckclaw.forge.atoms`.
- `duckclaw.forge.skills.github_bridge` fue removido. La integracion util de GitHub vive como capability generica en `duckclaw.github.mcp_bridge`, read-only por defecto y sin registro default desde factory.
- `duckclaw.github.workflow` fue removido. No debe existir workflow determinista de PR en core, ni manifests/heuristicas verticales acopladas a Quant, Job Hunter u otra vertical.
- `duckclaw.workers.identity` expone predicados genericos, no constantes `WORKER_*` ni sets de dominios.
- `duckclaw.workers.discovery` es el owner canonico de `list_workers`: combina solo el layout filesystem `default` con ids del catalogo DB-first y deja `duckclaw.workers.factory` como fachada compatible.
- `duckclaw.workers.visual_evidence_policy` es el owner canonico del limite de reintentos de evidencia visual en el grafo de worker; `workers.factory` conserva una fachada legacy compatible para callers existentes.
- `duckclaw.workers.tool_output_truncation` es el owner canonico del truncado puro de `ToolMessage` para contexto LLM: limita tool outputs largos, compacta outputs de sandbox removiendo `figure_base64` y aplica la compactacion existente de tools `reddit_*` sin convertir Reddit en vertical core. `workers.factory` conserva fachadas legacy compatibles para callers existentes.
- `duckclaw.workers.provider_input_budget` es el owner canonico del presupuesto de entrada por proveedor y helpers puros de poda de historial: normaliza `context_pruning_config`, estima tokens por mensajes, aplica limites Groq/MLX y divide historial preservando pares AI/tool. Sus limites configurables se leen DB-first desde `admin_runtime_settings` con dominio `runtime.provider_budget` (`groq.max_input_tokens`, `groq.tool_message_max_chars`, `mlx.max_input_tokens`, `mlx.tool_message_max_chars`); env vars quedan como fallback legacy si no hay DB/config. Depende de `duckclaw.workers.tool_output_truncation` para compactar `ToolMessage`; no decide routing ni ensamblaje de tools. `workers.factory` conserva fachadas legacy compatibles para callers existentes.
- `duckclaw.workers.context_monitor` es el owner canonico de la compresion/resumen de contexto del worker: serializa mensajes antiguos, compone el prompt generico de resumen, resuelve el LLM opcional de resumen y construye el nodo `context_monitor` a partir de `context_pruning_config`. No decide routing ni binding de tools; `workers.factory` solo lo conecta cuando la policy DB-first lo habilita y conserva fachadas legacy compatibles para helpers de resumen.
- `duckclaw.workers.tool_binding` es el owner canonico de helpers puros de superficie de tools para binding LLM: filtra tools de sandbox cuando el sandbox esta deshabilitado, reduce la superficie Groq generica removiendo `reddit_*`, construye `tool_choice` OpenAI-compatible y detecta tool calls recientes sin cambiar las rutas forzadas Reddit. No registra tools, no decide capabilities y no abre workers por filesystem; `workers.factory` conserva fachadas legacy compatibles para callers existentes.
- `duckclaw.runtime_session_settings` es el owner DB-first de flags runtime por chat/sesion. Usa `main.admin_runtime_settings` con dominio `runtime.session`, actor `chat:<chat_id>` y tenant explicito. Los flags `sandbox_enabled`, `sandbox_network_enabled` y overrides chat-scoped `llm_provider`/`llm_model`/`llm_base_url` ya no se leen/escriben desde `agent_config`; `/sandbox`, `/internet`, `/model`, `/setup`, el admin sandbox/playground, `workers.factory`, `graph_server` y `forge.schema` consumen este owner. La policy de red por chat acepta `tenant_id` explicito para evitar leer siempre el scope `default` en tenants no-default.
- `duckclaw.commands.model_setup` ya no escribe prompts de sistema en `agent_config` (`system_prompt` / `system_prompt_<worker>`). `/prompt <worker> --change ...` y `/setup system_prompt=...` hacen upsert de `main.prompt_policy_registry` con `policy_type='system_prompt'` y `policy_name=<worker_id>` mediante `UpsertPromptPolicyCommand`; `get_effective_system_prompt` resuelve desde `PromptPolicyResolver`.
- `duckclaw.workers.read_pool` ya no contiene special-cases por worker BI. Las restricciones de `SELECT *` sin `LIMIT` se modelan con la capability DB-first generica `bounded_select_star_read`; las restricciones de JSON remoto siguen bajo `bounded_json_read`.
- `duckclaw.workers.factory` ya no registra ni orquesta GitHub/Job Hunter como ramas especiales del core; esas integraciones solo pueden volver como capability/policy DB-first o extension externa.
- Los guardrails Markdown `capabilities/job_hunter.md` y `manager_tasks/job_*` fueron removidos del core. El runtime generico no debe cargar politicas de busqueda laboral desde archivos versionados como defaults.
- `packages/agents/src/duckclaw/graphs/sandbox.py` no contiene guias runtime verticales de Quant/Finanz/MQL5/ML4T/PyPortfolioOpt/broker/trading ni defaults laborales. La salida tabular del browser sandbox es generica (`rows_extracted` + Parquet bajo `/workspace/output/`) y la descripcion de browser solo habla de Playwright/sitios SPA de forma transversal.
- El layout de entrenamiento vive bajo `packages/agents/train/` con separacion de scripts de data, scripts de serve, datasets y outputs.
- El training puede tener prompts o datasets de dominio, pero no debe ser fuente de defaults runtime del core.

### Admin Y Playground

- Admin sigue el patron BFF/Gateway y debe leer/escribir DuckDB como control plane.
- Project detail y Playground respetan proyectos, equipos y conversaciones DB-first.
- `platform-orchestrator` ya no existe como worker hardcodeado, seed por actor ni plantilla protegida. El flujo conceptual se llama `managed workspace draft` / borrador administrado de workspace: el draft puede usar el layout permitido `default` y la confirmacion crea workers explicitos en `admin_worker_catalog` con `source_template_id='default'`.
- `/workspace/orchestrator/*` queda solo como alias de compatibilidad API para clientes existentes. Los DTOs, helpers, sesion interna, auditoria y copy UI deben usar naming `WorkspaceManagedDraft` / borrador administrado, no "orchestrator wizard" ni "Platform Orchestrator".
- Las instrucciones, fallback local, naming de borrador y metadatos de confirmacion del flujo administrado se resuelven desde `main.prompt_policy_registry` con la policy activa `manager_task/admin_workspace_managed_draft`, sembrada por migracion. Si la policy falta o es invalida, el endpoint debe fallar como configuracion administrable faltante; no debe reconstruir un prompt o identidad de dominio en Python.
- Playground ya no admite un worker especial para guiar proyectos. Con `project_id`, `default` se resuelve al primer agente asignado al proyecto; cualquier worker no-default debe venir del catalogo DB y pertenecer al proyecto.
- La config de Playground expone estado de voz como `voice.configured`, `voice.available` y `voice.tts_loaded`; la disponibilidad depende de `DUCKCLAW_SENSORY_BASE_URL` y health de Sensory TTS.

### Estado Actual Del Corte Admin DB-First

- Kanban de tarjetas ya salio de `services/api-gateway/routers/admin_db_first.py` hacia `services/api-gateway/routers/admin_domains/kanban.py`. El modulo usa `UpsertKanbanCardCommand` y `DeleteKanbanCardCommand`, abre el gateway DB en `read_only=True` para resolver perfil/existencia y devuelve `task_id` en cada mutacion.
- Kanban runtime vive aparte en `services/api-gateway/routers/admin_domains/kanban_runtime.py` y `admin.py` lo incluye como router de dominio.
- Prompt Policies ya salio de `admin_db_first.py` hacia `services/api-gateway/routers/admin_domains/prompt_policies.py`. El modulo expone CRUD y `/prompt-policies/health`, usa `UpsertPromptPolicyCommand` y `DeactivatePromptPolicyCommand`, y devuelve `task_id` en mutaciones.
- El health de prompt policies audita requirements con `prompt_policy_requirements_for_workers` y `missing_prompt_policies` contra el registry DB-first. La UI de policies ya muestra health, versiones y desactivacion suave de policies.
- Template contexts y catalog import ya salieron de `admin_db_first.py` hacia `services/api-gateway/routers/admin_domains/template_contexts.py`. El modulo valida visibilidad con gateway DB `read_only=True`, encola `ImportTemplatesToCatalogCommand`, `UpsertWorkerContextCommand`, `ReorderWorkerContextsCommand` y `DeactivateWorkerContextCommand`, y devuelve `task_id` en cada mutacion.
- Runtime Settings ya no escribe settings estructurados desde el gateway. `/settings/runtime`, `telegram.webhook_routes` y runtime config legacy de `agent_config` encolan comandos tipados (`UpsertRuntimeSettingCommand`, `UpsertAgentConfigEntriesCommand`, `DeleteAgentConfigEntriesCommand`) y devuelven `task_id`; las lecturas siguen DB-first con fallback env de bootstrap.
- `services/api-gateway/routers/admin.py` incluye routers de dominio para access management, auth, catalog skills, duckdb explorer, kanban, kanban runtime, playground chat, prompt policies, runtime config, sandbox sessions, template contexts, templates catalog, user agents, visual assets, workspace managed draft y workspace projects, y al final incluye el router transicional `admin_db_first.py`.
- Auth login ya no conserva DuckDB RW directo en el gateway. `_admin_auth_login_impl` lee usuarios/perfiles con `DuckClaw(... read_only=True)`, siembra el primer usuario con `UpsertConsoleUserCommand`, verifica credenciales sin mutar y delega fallos, limpieza de fallos y migracion de hash a comandos tipados.
- `services/api-gateway/routers/admin_db_first.py` bajo a una superficie transicional de runtime settings read/patch y knowledge/RAG. No debe crecer: cada nueva ruta admin debe ir a `admin_domains/*` o a un service compartido con comando tipado.
- Verificacion Corte 2: RED confirmado con 7 fallos esperados en runtime settings; GREEN focal `uv run pytest tests/test_admin_router.py tests/test_admin_router_split_static.py tests/test_admin_runtime_config.py tests/test_write_commands.py -q` => `116 passed, 1 warning`; dependientes `uv run pytest tests/test_admin_duckdb_explorer.py tests/test_admin_runtime_settings.py -q` => `17 passed`; regresion no-integration `uv run pytest tests/ -q -m "not integration" --ignore=tests/run_singleton_writer_pipeline.py --ignore=tests/deprecated` => `1358 passed, 2 skipped, 6 warnings`.

### Diferencias Entre `admin.py` Y `admin_db_first.py`

`services/api-gateway/routers/admin.py` es el router admin legacy/agregador. Sigue siendo el punto de montaje con `prefix="/api/v1/admin"`, dependencias de auth/admin key, health, overview, auditoria, ops y aliases historicos. Los mutators estructurados recientes salieron a `admin_domains/*` o comandos tipados; su rol actual es compatibilidad y agregacion, no recibir nuevos dominios grandes.

`services/api-gateway/routers/admin_db_first.py` es el router DB-first transicional para dominios que aun no fueron extraidos. Su evidencia actual esta acotada: knowledge/RAG conserva superficie transicional y runtime settings read/patch ya usa comandos tipados. Su rol correcto es vaciarse hacia `admin_domains/*` y hacia services compartidos que deleguen writes a `packages/shared/src/duckclaw/write_commands.py` + `write_command_handlers.py`.

Regla practica: `admin.py` agrega y conserva compatibilidad; `admin_db_first.py` contiene deuda transicional; `admin_domains/*` debe ser el destino de rutas por dominio; `packages/shared/src/duckclaw/*` debe ser el destino de contratos DB-first reutilizables por gateway, agentes y db-writer.

### Plan Prioritario Para DB-First Total

#### 1. Template Contexts Y Catalog Import

- Archivos objetivo: `services/api-gateway/routers/admin_db_first.py`, nuevo `services/api-gateway/routers/admin_domains/template_contexts.py` si se separan contexts, `packages/shared/src/duckclaw/write_commands.py`, `packages/shared/src/duckclaw/write_command_handlers.py`, `packages/shared/src/duckclaw/admin_template_import.py`, `packages/shared/src/duckclaw/admin_worker_catalog.py`.
- Estado actual: completado el corte de router y comandos. `import_templates_to_catalog`, `create_template_context`, `reorder_template_contexts` y `delete_template_context` viven en `admin_domains/template_contexts.py` y ya no abren `open_gateway_db(read_only=False)` desde el gateway.
- Comandos tipados implementados: `ImportTemplatesToCatalogCommand`, `UpsertWorkerContextCommand`, `ReorderWorkerContextsCommand` y `DeactivateWorkerContextCommand`.
- Handlers: `write_command_handlers.py` despacha esos comandos y reutiliza `admin_template_import` / `admin_worker_catalog`; no abre transacciones propias y valida que `worker_uid` pertenezca al `tenant_id` activo antes de mutar contextos.
- TDD focal: `tests/test_write_commands.py` cubre create/reorder/deactivate de contexts; `tests/test_admin_router_split_static.py` asegura que las rutas salieron de `admin_db_first.py`; `tests/test_admin_projects_ui_static.py::test_template_editor_explains_db_context_storage` sigue dentro de la verificacion focal.
- Aceptacion: toda mutacion de import/context devuelve `task_id`; el gateway solo usa `read_only=True` para resolver worker visible; no queda `BEGIN TRANSACTION` en rutas de contexts/import; el editor admin conserva BFF y sigue explicando `main.admin_worker_contexts`.
- Verificacion e2e: desde Admin Templates importar un worker de `default`, crear/reordenar/desactivar un contexto, confirmar que aparece en detalle del template y que el DB-writer marca el `task_id` como `success`.

#### 2. Runtime Settings

- Archivos objetivo: `services/api-gateway/routers/admin_db_first.py`, `services/api-gateway/routers/admin.py`, `services/api-gateway/routers/admin_domains/runtime_config.py`, `packages/shared/src/duckclaw/write_commands.py`, `packages/shared/src/duckclaw/write_command_handlers.py`, `packages/shared/src/duckclaw/admin_runtime_settings.py`.
- Estado actual: Corte 2 completado. `patch_runtime_settings` abre el gateway DB solo en `read_only=True` para resolver perfil, encola `UpsertRuntimeSettingCommand` por setting, devuelve `task_id`/`task_ids` y no llama `upsert_runtime_setting` directo. `admin.py` encola `telegram.webhook_routes` con `UpsertRuntimeSettingCommand` y devuelve `task_id`. `runtime_config.py` reemplazo SQL raw contra `agent_config` por `UpsertAgentConfigEntriesCommand` y `DeleteAgentConfigEntriesCommand`.
- Comandos tipados implementados: `UpsertRuntimeSettingCommand` ahora soporta `value_json` y `updated_by`; `DeleteAgentConfigEntriesCommand` cubre deletes legacy idempotentes. No se implemento `DeleteRuntimeSettingCommand` porque el alcance actual no expone borrado de settings runtime.
- Handlers: `write_command_handlers.py` preserva `value_json`, `value_kind`, `secret`, `updated_by`, aplica upserts de `agent_config` idempotentes y borra keys legacy sin error al reintentar. Los handlers no abren transacciones; el caller DB-writer/inline conserva el limite transaccional.
- TDD focal: RED confirmado con fallos esperados para `task_id`, ausencia de `value_json`, ausencia de `DeleteAgentConfigEntriesCommand`, direct writes y SQL raw. GREEN: `tests/test_admin_router.py`, `tests/test_admin_router_split_static.py`, `tests/test_admin_runtime_config.py`, `tests/test_write_commands.py`, `tests/test_admin_duckdb_explorer.py` y `tests/test_admin_runtime_settings.py` pasan en focal; regresion no-integration completa queda en `1358 passed, 2 skipped`.
- Aceptacion: runtime settings y Telegram routes no abren `open_gateway_db(read_only=False)` en sus mutators; runtime config legacy no encola payload `"query"` ni construye `INSERT`/`DELETE` raw; mutaciones devuelven `task_id`; lecturas siguen resolviendo DB-first con fallback env solo para bootstrap.
- Verificacion e2e pendiente: cambiar `mcp.port`, `telegram.webhook_routes` y una setting de `runtime.session` contra db-writer real, observar `task_status:<task_id>=success`, reiniciar gateway si el setting requiere reload y confirmar UI/resolvers.

#### 3. Workspace Projects Y Project Agents

- Archivos objetivo: `services/api-gateway/routers/admin_db_first.py`, nuevo `services/api-gateway/routers/admin_domains/workspace_projects.py`, `packages/shared/src/duckclaw/admin_workspace.py`, `packages/shared/src/duckclaw/write_commands.py`, `packages/shared/src/duckclaw/write_command_handlers.py`.
- Estado actual: Corte 3 completado. Las rutas `/workspace/projects*` viven en `admin_domains/workspace_projects.py`; `admin_db_first.py` ya no contiene `WorkspaceProjectBody`, `ProjectAgentBody` ni handlers de proyectos. El gateway usa `open_gateway_db(read_only=True)` para list/detail, snapshots, perfil y worker visible; crear/desactivar/reactivar/eliminar/attach/detach encolan comandos tipados y devuelven `task_id`.
- Comandos tipados implementados: `CreateProjectCommand` conserva `visibility`; `SetProjectStatusCommand`, `DeleteProjectCommand` y `DetachAgentFromProjectCommand` cubren status/delete/detach. `AssignAgentToProjectCommand` sigue siendo el comando canonico para attach.
- Handlers: `write_command_handlers.py` despacha `set_project_status`, `delete_project` y `detach_agent_from_project`; valida tenant del proyecto, owner para status/delete, owner/miembro para attach/detach y que `worker_uid` pertenezca al mismo tenant. Los handlers son idempotentes y no abren transacciones propias.
- TDD focal: RED confirmado con fallos esperados por modulo faltante, comandos/handlers ausentes y respuestas sin `task_id`. GREEN: `uv run pytest tests/test_admin_router.py tests/test_admin_router_split_static.py tests/test_admin_projects_ui_static.py tests/test_write_commands.py tests/test_admin_workspace_catalog.py -q` queda en `165 passed, 1 warning`.
- Aceptacion: los mutators de proyectos devuelven `task_id`; las lecturas siguen paginadas y read-only; no hay `open_gateway_db(read_only=False)` ni `BEGIN TRANSACTION` en el router de proyectos; la UI conserva filtros active/inactive/all y acciones reversibles.
- Verificacion e2e: crear proyecto, asignar agente visible, desactivar/reactivar, eliminar, verificar que Playground con `project_id` fuerza worker perteneciente al proyecto.

#### 4. Managed Workspace Draft Confirm

- Archivos objetivo: `services/api-gateway/routers/admin_db_first.py`, nuevo `services/api-gateway/routers/admin_domains/workspace_managed_draft.py`, `packages/shared/src/duckclaw/write_commands.py`, `packages/shared/src/duckclaw/write_command_handlers.py`, `packages/shared/src/duckclaw/admin_workspace.py`, `packages/shared/src/duckclaw/admin_worker_catalog.py`.
- Estado actual: Corte 4 completado. `WorkspaceManagedDraft` salio de `admin_db_first.py` hacia `services/api-gateway/routers/admin_domains/workspace_managed_draft.py`; los aliases publicos `/workspace/orchestrator/draft` y `/workspace/orchestrator/confirm` se conservan solo por compatibilidad API. Draft sigue leyendo policy/skills/catalog DB-first con `open_gateway_db(read_only=True)`.
- Comandos tipados implementados: `ConfirmWorkspaceManagedDraftCommand` confirma proyecto, workers, snapshots/versiones, contexto compartido y asignaciones como una unica mutacion tipada con `task_id`.
- Handlers: `write_command_handlers.py` despacha `confirm_workspace_managed_draft`; valida actor/tenant, normaliza `worker_id`, upsertea proyecto/workers, crea versiones/contextos/asignaciones de forma idempotente y no abre transacciones propias. El reintento del mismo payload no duplica proyecto, worker, version, contexto ni assignment.
- TDD focal: RED confirmado con 5 fallos esperados por modulo/comando ausente y endpoint sin `task_id`. GREEN minimo: `uv run pytest tests/test_admin_router_split_static.py::test_managed_workspace_draft_routes_live_in_domain_module tests/test_admin_router_split_static.py::test_managed_workspace_draft_router_uses_typed_commands_only tests/test_admin_workspace_catalog.py::test_orchestrator_confirm_creates_project_workers_context_and_assignments tests/test_write_commands.py::TestWriteCommands::test_confirm_workspace_managed_draft_command_roundtrip tests/test_write_commands.py::TestCommandHandlers::test_confirm_workspace_managed_draft_handler_is_idempotent -q` => `5 passed, 1 warning`.
- Verificacion focal: `uv run pytest tests/test_admin_workspace_catalog.py tests/test_admin_router_split_static.py tests/test_admin_projects_ui_static.py tests/test_write_commands.py -q` => `121 passed, 1 warning`.
- Aceptacion: `/workspace/orchestrator/confirm` devuelve `task_id`; el gateway no usa `open_gateway_db(read_only=False)` ni `BEGIN TRANSACTION` para Managed Workspace Draft; `admin_db_first.py` ya no contiene DTOs/helpers/rutas `WorkspaceManagedDraft`; naming interno sigue `WorkspaceManagedDraft` y no depende de `platform-orchestrator`.
- Verificacion e2e pendiente: abrir wizard, generar draft con LLM o fallback, confirmar contra DB-writer real, observar `task_status:<task_id>=success`, ver proyecto/workers en catalogo y abrir Playground con worker perteneciente al proyecto.

#### 5. User Agents, Catalog Skills Y Playground Model Settings

- Archivos objetivo: `services/api-gateway/routers/admin.py`, `services/api-gateway/routers/admin_db_first.py`, `services/api-gateway/routers/admin_domains/playground_chat.py`, `apps/duckclaw-admin/src/services/adminService.ts`, `packages/shared/src/duckclaw/write_commands.py`, `packages/shared/src/duckclaw/write_command_handlers.py`.
- Estado actual: Corte 5A completado para `/user-agents`. `create_user_agent` salio de `admin_db_first.py` hacia `services/api-gateway/routers/admin_domains/user_agents.py`; el gateway solo abre `open_gateway_db(read_only=True)` para resolver perfil/actor, encola `UpsertUserAgentCommand` y devuelve `task_id` junto con `agent` para compatibilidad UI. El endpoint ya no crea archivos runtime fisicos ni manifiestos fuera de DB; expone `manifest_path` como `db://admin_worker_catalog/<worker_uid>/manifest.json`.
- Estado actual: Corte 5B completado para Catalog Skills. `/catalog/skills` salio de `admin.py` hacia `services/api-gateway/routers/admin_domains/catalog_skills.py`; las lecturas usan gateway DB `read_only=True`, `POST /catalog/skills` encola `UpsertCatalogSkillCommand`, `DELETE /catalog/skills/{name}` encola `DeactivateCatalogSkillCommand`, y ambos devuelven `task_id`.
- Estado actual: Corte 5C completado para Playground Model Settings. `PUT /playground/model` ya no llama `execute_model` con `DuckClaw(... read_only=False)` ni espeja overrides a `agent_config` de la boveda; encola `UpsertRuntimeSettingCommand` para `runtime.session.llm_provider`, `runtime.session.llm_model` y `runtime.session.llm_base_url` con `actor_email=chat:<chat_id>`, devuelve `task_id`/`task_ids`, y la UI resuelve el LLM efectivo desde `admin_runtime_settings`.
- Comandos tipados implementados: `UpsertUserAgentCommand` crea/actualiza la fila compatible `main.admin_user_agents`, el worker en `main.admin_worker_catalog`, su version en `main.admin_worker_versions` y el contexto de prompt en `main.admin_worker_contexts` bajo una transaccion administrada por caller. `UpsertCatalogSkillCommand` y `DeactivateCatalogSkillCommand` upsertean/desactivan skills de `main.admin_skills` por actor/tenant/nombre y son idempotentes bajo reintento.
- TDD focal Corte 5A: RED confirmado con fallos esperados por router inexistente, endpoint sin `task_id`, comando ausente y handler no registrado. GREEN: `uv run pytest tests/test_admin_user_agents.py tests/test_admin_router_split_static.py tests/test_admin_projects_ui_static.py tests/test_write_commands.py -q` => `96 passed, 1 warning`.
- Aceptacion Corte 5A: crear user agent devuelve `task_id`, no hay `open_gateway_db(read_only=False)` ni `BEGIN TRANSACTION` en `admin_domains/user_agents.py`, `/user-agents` ya no vive en `admin_db_first.py`, y el runtime catalog/playground sigue listando el agente desde DB-first.
- TDD focal Corte 5B: RED confirmado con 6 fallos esperados por comandos/handler/router ausentes y endpoint sin `task_id`. GREEN minimo `uv run pytest tests/test_write_commands.py::TestWriteCommands::test_catalog_skill_commands_roundtrip tests/test_write_commands.py::TestCommandHandlers::test_catalog_skill_handler_upserts_and_deactivates_idempotently tests/test_admin_router_split_static.py::test_catalog_skill_routes_live_in_domain_module tests/test_admin_router_split_static.py::test_catalog_skill_router_uses_typed_commands_only tests/test_admin_router.py::test_create_catalog_skill_for_authenticated_actor tests/test_admin_router.py::test_deactivate_catalog_skill_for_authenticated_actor -q` => `6 passed, 1 warning`.
- TDD focal Corte 5C: RED confirmado con 2 fallos esperados por respuesta sin `task_id` y `DuckClaw(gw, read_only=False)`/`execute_model` en `/playground/model`. GREEN minimo `uv run pytest tests/test_admin_playground_model.py::test_playground_set_model_provider tests/test_admin_playground_model.py::test_playground_config_reflects_chat_override tests/test_admin_router_split_static.py::test_playground_model_settings_use_runtime_setting_commands_only -q` => `3 passed, 1 warning`.
- Verificacion focal Corte 5B/5C: `uv run pytest tests/test_write_commands.py tests/test_admin_router.py tests/test_admin_router_split_static.py tests/test_admin_playground_model.py tests/test_admin_duckdb_explorer.py tests/test_db_first_guardrails_static.py -q` => `154 passed, 1 warning`.
- Verificacion e2e: crear skill global privada, crear user agent desde default, seleccionar proyecto en Playground, cambiar provider/model y enviar mensaje con worker perteneciente al proyecto.

#### 6. Legacy SQL / Raw Config Paths

- Archivos objetivo: `services/api-gateway/routers/admin_domains/runtime_config.py`, `services/api-gateway/routers/admin_domains/duckdb_explorer.py`, `services/api-gateway/main.py`, `packages/shared/src/duckclaw/write_commands.py`, `packages/shared/src/duckclaw/write_command_handlers.py`, `packages/agents/src/duckclaw/graphs/on_the_fly_commands.py`.
- Estado actual: Corte 6 guardrail completado. `runtime_config.py` ya no crea SQL raw para `agent_config`; usa `UpsertAgentConfigEntriesCommand` y `DeleteAgentConfigEntriesCommand`. Las lecturas RW triviales detectadas en `_list_templates_impl`, `playground_config` y `_duckdb_actor_scope` pasaron a `read_only=True`.
- Estado actual: Corte 6B completado. Los mutators de templates (`PUT /templates/{worker_id}/files/{file_path}`, `DELETE /templates/{worker_id}`, `POST /templates/{worker_id}/reactivate`, `DELETE /templates/{worker_id}/hard-delete`) viven en `services/api-gateway/routers/admin_domains/templates_catalog.py`, encolan `UpdateCatalogWorkerFileCommand`, `DeactivateCatalogWorkerCommand`, `ReactivateCatalogWorkerCommand` y `HardDeleteCatalogWorkerCommand`, devuelven `task_id` y ya no abren `open_gateway_db(read_only=False)` desde gateway. Los helpers legacy equivalentes en `admin.py` quedaron como 410 para evitar reutilizacion accidental.
- Estado actual: Corte 6C completado. `duckdb_drop_legacy_schemas` valida allowlists DB-first/env en read-only, encola `DropLegacyDuckDbObjectsCommand` contra la boveda resuelta y ya no ejecuta `BEGIN TRANSACTION` ni `DROP` directo en el gateway; la transaccion vive en db-writer o inline typed handler.
- Estado actual: Corte Auth DB-first completado. `services/api-gateway/routers/admin.py::_admin_auth_login_impl` ya no abre `DuckClaw(... read_only=False)`: seed inicial usa `UpsertConsoleUserCommand`; la verificacion usa `authenticate_console_user_readonly`; los fallos usan `RecordAdminLoginFailureCommand`; el exito usa `ClearAdminLoginFailuresCommand`; y la migracion PBKDF2/rehash usa `UpdateConsoleUserPasswordHashCommand`. Las sesiones continuan en Redis y no agregan tabla DuckDB nueva.
- Deuda residual allowlisted: `services/api-gateway/core/fly_command_invocation.py::_open_legacy_fly_duckclaw_rw` conserva `DuckClaw(... read_only=False)` para compatibilidad de slash/fly commands heredados que aun no aceptan handle read-only + comandos tipados; `services/api-gateway/routers/db_write_compat.py::enqueue_write` conserva payload `"query"` exclusivamente para `/api/v1/db/write` raw legacy/smoke pipeline, con comentario auditado en codigo. No hay `open_gateway_db(read_only=False)` ni `BEGIN TRANSACTION` en `services/api-gateway`, y `main.py::_invoke_chat` ya no contiene la apertura RW directa.
- Comandos tipados implementados en estos cortes: `UpdateCatalogWorkerFileCommand`, `DeactivateCatalogWorkerCommand`, `ReactivateCatalogWorkerCommand`, `HardDeleteCatalogWorkerCommand`, `DropLegacyDuckDbObjectsCommand`, `RecordAdminLoginFailureCommand`, `ClearAdminLoginFailuresCommand`, `UpdateConsoleUserPasswordHashCommand` y `ForgetChatStateCommand`, con dispatch en `packages/shared/src/duckclaw/write_command_handlers.py`.
- Handlers: raw SQL queda solo para `RawSqlCommand`/`admin_sql` y `routers/db_write_compat.py::enqueue_write` con compatibilidad estrecha; no se debe usar para config runtime, catalog skills, templates, cleanup legacy, model settings ni mutaciones UI estructuradas.
- TDD focal Corte 6: RED confirmado con 3 fallos esperados por lecturas RW fuera del allowlist y nombres reales de compat. GREEN `uv run pytest tests/test_db_first_guardrails_static.py -q` => `4 passed`; verificacion focal ampliada `uv run pytest tests/test_write_commands.py tests/test_admin_router.py tests/test_admin_router_split_static.py tests/test_admin_playground_model.py tests/test_admin_duckdb_explorer.py tests/test_db_first_guardrails_static.py -q` => `154 passed, 1 warning`.
- TDD focal Corte 6B/6C: RED confirmado con 11 fallos esperados por mutators legacy de templates, cleanup con transaccion gateway, comandos/handlers ausentes y endpoint sin `task_id`. GREEN focal `uv run pytest tests/test_db_first_guardrails_static.py tests/test_admin_router_split_static.py tests/test_write_commands.py::TestWriteCommands::test_template_catalog_mutation_commands_roundtrip tests/test_write_commands.py::TestWriteCommands::test_drop_legacy_duckdb_objects_command_roundtrip tests/test_write_commands.py::TestCommandHandlers::test_template_catalog_handlers_mutate_actor_owned_worker tests/test_write_commands.py::TestCommandHandlers::test_drop_legacy_duckdb_objects_handler_drops_requested_objects tests/test_admin_router.py::test_template_file_mutator_enqueues_typed_command -q` => `37 passed, 1 warning`; `uv run pytest tests/test_admin_duckdb_explorer.py -q` => `14 passed`.
- Verificacion final Corte 6B/6C: `uv run pytest tests/test_db_first_guardrails_static.py tests/test_admin_router_split_static.py tests/test_write_commands.py tests/test_admin_router.py tests/test_admin_duckdb_explorer.py -q` => `161 passed, 1 warning`; regresion no-integration `uv run pytest tests/ -q -m "not integration" --ignore=tests/run_singleton_writer_pipeline.py --ignore=tests/deprecated` => `1392 passed, 2 skipped, 6 warnings`.
- TDD focal Corte Auth DB-first: RED confirmado con 6 fallos esperados por allowlist RW, comandos/handlers ausentes y login sin enqueue tipado. GREEN minimo `uv run pytest tests/test_db_first_guardrails_static.py::test_gateway_duckclaw_read_write_is_limited_to_explicit_runtime_compat tests/test_write_commands.py::TestWriteCommands::test_console_user_commands_roundtrip tests/test_write_commands.py::TestCommandHandlers::test_admin_auth_state_commands_apply_idempotently tests/test_admin_auth_sessions.py::test_login_success_clears_failures_with_typed_command tests/test_admin_auth_sessions.py::test_login_failure_records_failure_with_typed_command tests/test_admin_access.py::test_admin_login_seeds_default_when_table_empty -q` => `6 passed`; focal ampliado `uv run pytest tests/test_admin_auth_sessions.py tests/test_admin_login_static.py tests/test_admin_router.py tests/test_admin_access.py tests/test_db_first_guardrails_static.py tests/test_write_commands.py -q` => `147 passed, 4 warnings`; regresion no-integration `uv run pytest tests/ -q -m "not integration" --ignore=tests/run_singleton_writer_pipeline.py --ignore=tests/deprecated` => `1396 passed, 2 skipped, 6 warnings`.
- Aceptacion: nuevos guardrails bloquean `open_gateway_db(read_only=False)`, `BEGIN TRANSACTION`, payload `"query"` raw y `enqueue_duckdb_write_sync` fuera de allowlists auditadas. Las excepciones temporales quedan documentadas con ruta/funcion concreta.
- Verificacion e2e: ejecutar `/model`, `/sandbox`, `/heartbeat`, `/vault` y una lectura `/health`; confirmar que las mutaciones generan `task_id` o pasan por DB-writer, y que el playground/admin UI no pierde BFF.

### Gateway `main.py` De-God

- Corte nuevo completado: `/api/v1/db/write` salio de `services/api-gateway/main.py` hacia `services/api-gateway/routers/db_write_compat.py`. `main.py` conserva el montaje del router, pero ya no declara `WriteRequest`, `EnqueueResponse`, `enqueue_write` ni el payload raw `"query"`.
- El owner nuevo es intencionalmente de compatibilidad: acepta solo el endpoint publico legado `/api/v1/db/write`, rechaza `SELECT`, resuelve boveda activa/dedicada como antes, valida `db_path` explicito y encola en `duckdb_write_queue` para el singleton writer. Este owner no debe usarse para mutaciones estructuradas admin ni runtime config; esas siguen obligadas a comandos tipados.
- Guardrail nuevo: `tests/test_db_first_guardrails_static.py` exige que el payload raw `"query"` viva solo en `routers/db_write_compat.py::enqueue_write`, que el comentario auditado este en ese owner y que `main.py` no vuelva a declarar `enqueue_write`.
- Verificacion focal del corte: RED confirmado con 3 fallos esperados cuando el owner seguia en `main.py`; GREEN `uv run pytest tests/test_db_first_guardrails_static.py::test_gateway_raw_query_payloads_are_limited_to_compat_enqueue tests/test_db_first_guardrails_static.py::test_legacy_raw_query_enqueue_has_audited_compat_comment tests/test_db_first_guardrails_static.py::test_gateway_main_no_longer_owns_legacy_raw_query_enqueue -q` => `3 passed`; contrato compat `uv run pytest tests/run_singleton_writer_pipeline.py::test_gateway_rejects_select_queries tests/run_singleton_writer_pipeline.py::test_gateway_accepts_insert_returns_202_and_task_id tests/run_singleton_writer_pipeline.py::test_gateway_accepts_create_table_returns_202 -q` => `3 passed`; focal consolidado `uv run pytest tests/test_db_first_guardrails_static.py tests/run_singleton_writer_pipeline.py::test_gateway_rejects_select_queries tests/run_singleton_writer_pipeline.py::test_gateway_accepts_insert_returns_202_and_task_id tests/run_singleton_writer_pipeline.py::test_gateway_accepts_create_table_returns_202 -q` => `10 passed`; regresion no-integration `uv run pytest tests/ -q -m "not integration" --ignore=tests/run_singleton_writer_pipeline.py --ignore=tests/deprecated` => `1393 passed, 2 skipped, 6 warnings`.
- Corte slash/fly extraction completado: `main.py::_invoke_chat` ya no abre `DuckClaw(... read_only=False)` ni importa `handle_command`; delega los slash/fly commands a `services/api-gateway/core/fly_command_invocation.py::invoke_legacy_fly_command`. El owner nuevo concentra resolucion/audit de boveda fly, limpieza de cache de worker graphs, invocacion legacy, envio de charts y artifacts admin, manteniendo el contrato publico de chat/playground.
- Corte `/forget` DB-first completado: `ForgetChatStateCommand` borra de forma idempotente `telegram_conversation` para chat ids numericos, `api_conversation` para session ids string y la key `chat_<id>_last_audit` en `agent_config`. `execute_forget` usa `enqueue_typed_command` cuando el handle es read-only, hace polling breve para conservar confirmacion inmediata y mantiene la respuesta legacy `✅ Historial borrado.`.
- La excepcion RW quedo sin miembros despues del corte HITL, aunque `core/fly_command_invocation.py::_open_legacy_fly_duckclaw_rw` se conserva como compatibilidad auditada por ahora. `_open_fly_duckclaw` solo puede abrir RW si el comando esta explicitamente en `LEGACY_RW_FLY_COMMANDS`; comandos no listados caen a `DuckClaw(read_only=True)`.
- Guardrail nuevo slash/fly: `tests/test_db_first_guardrails_static.py` exige que el unico `DuckClaw(... read_only=False)` del gateway viva en `core/fly_command_invocation.py::_open_legacy_fly_duckclaw_rw`, que `main.py` solo delegue a `invoke_legacy_fly_command`, que el owner tenga comentario auditado, que los comandos migrados (`/context`, `/workers`, `/forget`, `/goals`, `/meditate`, `/comfyui`, `/reject-code`, `/reject_code`, `/resolve-uncertainty`, `/resolve_uncertainty`) no vuelvan a `LEGACY_RW_FLY_COMMANDS` y que la allowlist enumere solo los pendientes actuales documentados.
- Verificacion focal Corte slash/fly extraction: RED confirmado con 2 fallos esperados por allowlist apuntando al owner nuevo inexistente y por `main.py::_invoke_chat` todavia conteniendo `DuckClaw(... read_only=False)`; GREEN `uv run pytest tests/test_db_first_guardrails_static.py::test_gateway_duckclaw_read_write_is_limited_to_explicit_runtime_compat tests/test_db_first_guardrails_static.py::test_gateway_main_delegates_legacy_fly_rw_exception_to_owner -q` => `2 passed`. Contratos chat/playground focales: `uv run pytest tests/test_admin_router.py::test_invoke_chat_admin_console_delivery_bypasses_telegram_guard -q` => `1 passed, 1 warning`; `uv run pytest tests/test_admin_router.py::test_playground_chat tests/test_admin_router.py::test_playground_chat_rejects_worker_outside_team tests/test_admin_workspace_catalog.py::test_playground_config_and_chat_support_db_first_project_scope -q` => `3 passed, 1 warning`.
- Verificacion Corte `/forget`: RED confirmado con 6 fallos esperados por comando/handler ausente, allowlist RW y contrato RO faltante. GREEN focal `uv run pytest tests/test_fly_command_invocation.py tests/test_on_the_fly_forget.py tests/test_write_commands.py::TestWriteCommands::test_forget_chat_state_command_roundtrip tests/test_write_commands.py::TestCommandHandlers::test_forget_chat_state_handler_deletes_conversations_and_audit_idempotently tests/test_db_first_guardrails_static.py::test_gateway_fly_rw_exception_excludes_read_only_safe_commands -q` => `11 passed, 1 warning`. Focal ampliado `uv run pytest tests/test_commands_chat_state_contract.py tests/test_on_the_fly_forget.py tests/test_fly_command_invocation.py tests/test_api_gateway.py::test_forget_command_via_api_succeeds tests/test_db_first_guardrails_static.py tests/test_forge_legacy_cleanup.py tests/test_write_commands.py -q` => `122 passed, 1 warning`. Regresion no-integration `uv run pytest tests/ -q -m "not integration" --ignore=tests/run_singleton_writer_pipeline.py --ignore=tests/deprecated` => `1410 passed, 2 skipped, 6 warnings`.
- Corte slash/fly batch DB-first completado: `/crons` ya no necesita handle RW para su schedule primario; `duckclaw.commands.crons` usa `_set_chat_state_entries` para escribir `agent_config` directamente solo con handle RW heredado, o encolar `UpsertAgentConfigEntriesCommand` + polling breve cuando recibe handle read-only. `READ_ONLY_SAFE_FLY_COMMANDS` ahora cubre `/audit`, `/crons`, `/heartbeat`, `/health`, `/history`, `/internet`, `/llm`, `/model`, `/models`, `/network`, `/prompt`, `/provider`, `/red`, `/sandbox`, `/sandox`, `/setup`, `/system`, `/system_prompt`, `/team` y `/vault` ademas de `/context`, `/workers` y `/forget`; esos nombres ya no aparecen en `LEGACY_RW_FLY_COMMANDS`.
- Corte slash/fly `/team` + `/vault` completado: `/team` ya estaba acotado a `duckclaw.commands.team_access` y usa `UpsertAuthorizedUserCommand`, `DeleteAuthorizedUserCommand`, `UpsertSharedDbGrantCommand` y `DeleteSharedDbGrantCommand`; el ajuste de este corte asegura que el handle read-only se libere/reanude alrededor de `enqueue_typed_command` para evitar lock contention con DuckDB. `/vault` queda read-only-safe en el gateway porque `execute_vault` recibe la ruta del fly handle y, en ese modo, solo muestra la bóveda fija o rechaza subcomandos del registry con mensaje de compatibilidad; no crea, cambia ni elimina bóvedas desde ese camino.
- Estado actual de `LEGACY_RW_FLY_COMMANDS`: ninguno.
- TDD focal Corte slash/fly batch: RED confirmado con fallo esperado por `/crons --delta` ejecutando SQL directo en handle read-only y por allowlist que abria el batch en RW. GREEN minimo `uv run pytest tests/test_commands_crons_contract.py::test_crons_delta_with_read_only_handle_queues_typed_agent_config_command tests/test_fly_command_invocation.py::test_db_first_fly_command_batch_is_read_only_safe tests/test_fly_command_invocation.py::test_db_first_fly_command_batch_opens_read_only_duckclaw -q` => `37 passed, 1 warning`; focal de contratos `uv run pytest tests/test_commands_crons_contract.py tests/test_fly_command_invocation.py tests/test_commands_runtime_toggles_contract.py tests/test_commands_health_contract.py tests/test_commands_model_setup_contract.py tests/test_commands_history_contract.py tests/test_commands_chat_state_contract.py tests/test_on_the_fly_forget.py -q` => `74 passed, 1 warning`; focal ampliado DB-first/SOA `uv run pytest tests/test_write_commands.py tests/test_commands_crons_contract.py tests/test_fly_command_invocation.py tests/test_goals_delta.py tests/test_db_first_guardrails_static.py -q` => `144 passed, 1 warning`; regresion no-integration `uv run pytest tests/ -q -m "not integration" --ignore=tests/run_singleton_writer_pipeline.py --ignore=tests/deprecated` => `1448 passed, 2 skipped, 6 warnings`.
- TDD focal Corte `/team` + `/vault`: RED confirmado con 5 fallos esperados por `/team` y `/vault` fuera de `READ_ONLY_SAFE_FLY_COMMANDS`, apertura RW del fly handle y modulo SOA `write_handlers.access` ausente; RED adicional de `/team --add` confirmo que el comando encolaba tipado pero no liberaba el handle RO. GREEN minimo `uv run pytest tests/test_fly_command_invocation.py::test_db_first_fly_command_batch_is_read_only_safe tests/test_fly_command_invocation.py::test_db_first_fly_command_batch_opens_read_only_duckclaw tests/test_write_commands.py::TestCommandHandlers::test_access_write_handlers_live_in_domain_module -q` => `41 passed, 1 warning`; contrato `/team` RO `uv run pytest tests/test_commands_team_access_contract.py::test_team_add_with_read_only_handle_queues_typed_command_and_releases_handle -q` => `1 passed, 1 warning`; focal combinado `uv run pytest tests/test_fly_command_invocation.py tests/test_commands_team_access_contract.py tests/test_commands_vaults_contract.py tests/test_write_commands.py::TestCommandHandlers::test_access_write_handlers_live_in_domain_module tests/test_write_commands.py::TestCommandHandlers::test_authorized_user_handlers_upsert_and_delete tests/test_write_commands.py::TestCommandHandlers::test_shared_grant_handlers_upsert_and_delete -q` => `55 passed, 1 warning`; focal ampliado `uv run pytest tests/test_fly_command_invocation.py tests/test_commands_team_access_contract.py tests/test_commands_vaults_contract.py tests/test_db_first_guardrails_static.py tests/test_write_commands.py -q` => `130 passed, 1 warning`; regresion no-integration `uv run pytest tests/ -q -m "not integration" --ignore=tests/run_singleton_writer_pipeline.py --ignore=tests/deprecated` => `1454 passed, 2 skipped, 6 warnings`.
- Corte SOA `write_handlers.workers` completado: los handlers de `upsert_worker`, `upsert_user_agent`, catalog skills, catalog worker file/status/delete, template import/contexts y `upsert_worker_capability` salieron de `packages/shared/src/duckclaw/write_command_handlers.py` hacia `packages/shared/src/duckclaw/write_handlers/workers.py`. `write_command_handlers.py` conserva la fachada importable y el dispatcher, pero el owner canonico es el modulo de dominio; los tests validan `__module__ == "duckclaw.write_handlers.workers"` y que el modulo no administre transacciones propias.
- Verificacion focal SOA workers: RED confirmado con `ImportError: cannot import name 'workers' from 'duckclaw.write_handlers'`; GREEN `uv run pytest tests/test_write_commands.py -q -k "worker_catalog_write_handlers_live_in_domain_module or upsert_worker or worker_context_handlers_create_reorder_and_deactivate or catalog_skill or user_agent"` => `14 passed, 56 deselected`; lints de `write_handlers/workers.py`, `write_command_handlers.py` y `tests/test_write_commands.py` sin errores.
- Corte SOA `write_handlers.workspace` completado: los handlers de `create_project`, `add_project_member`, `assign_agent_to_project`, `set_project_status`, `delete_project`, `detach_agent_from_project` y `confirm_workspace_managed_draft` salieron de `packages/shared/src/duckclaw/write_command_handlers.py` hacia `packages/shared/src/duckclaw/write_handlers/workspace.py`. `write_command_handlers.py` conserva solo fachada/imports compatibles y el dispatcher; las reglas de acceso owner/member, tenant matching y reintentos idempotentes siguen en el modulo de dominio.
- Verificacion focal SOA workspace: RED confirmado con `ImportError: cannot import name 'workspace' from 'duckclaw.write_handlers'`; GREEN `uv run pytest tests/test_write_commands.py -q -k "workspace_write_handlers_live_in_domain_module or create_project or add_project_member or assign_agent_to_project or project_status_handler or delete_project_handler or detach_agent_handler or confirm_workspace_managed_draft"` => `13 passed, 58 deselected`; lints de `write_handlers/workspace.py`, `write_command_handlers.py` y `tests/test_write_commands.py` sin errores.
- Corte SOA `write_handlers.kanban` completado: los handlers de `upsert_kanban_card`, `delete_kanban_card` y sus helpers de eventos/tags salieron de `packages/shared/src/duckclaw/write_command_handlers.py` hacia `packages/shared/src/duckclaw/write_handlers/kanban.py`. El dispatcher conserva compatibilidad de imports y los eventos `kanban_card.created/updated/deleted` siguen dentro de la transaccion administrada por caller.
- Verificacion focal SOA Kanban: RED confirmado con `ImportError: cannot import name 'kanban' from 'duckclaw.write_handlers'`; GREEN `uv run pytest tests/test_write_commands.py -q -k "kanban_write_handlers_live_in_domain_module or upsert_kanban_card or delete_kanban_card"` => `3 passed, 69 deselected`; lints de `write_handlers/kanban.py`, `write_command_handlers.py` y `tests/test_write_commands.py` sin errores.
- Corte SOA `write_handlers.admin_auth` completado: los handlers de `upsert_console_user`, `deactivate_console_user`, `record_admin_login_failure`, `clear_admin_login_failures` y `update_console_user_password_hash` salieron de `packages/shared/src/duckclaw/write_command_handlers.py` hacia `packages/shared/src/duckclaw/write_handlers/admin_auth.py`. El dispatcher conserva compatibilidad de imports y la logica sigue delegando en `duckclaw.admin_console_users`.
- Verificacion focal SOA admin auth: RED confirmado con `ImportError: cannot import name 'admin_auth' from 'duckclaw.write_handlers'`; GREEN `uv run pytest tests/test_write_commands.py -q -k "admin_auth_write_handlers_live_in_domain_module or console_user_commands_apply_and_deactivate or admin_auth_state_commands_apply_idempotently"` => `3 passed, 70 deselected`; lints de `write_handlers/admin_auth.py`, `write_command_handlers.py` y `tests/test_write_commands.py` sin errores.
- Corte SOA `write_handlers.prompt_policies` completado: los handlers de `upsert_prompt_policy`, `deactivate_prompt_policy` y sus helpers de normalizacion/checksum salieron de `packages/shared/src/duckclaw/write_command_handlers.py` hacia `packages/shared/src/duckclaw/write_handlers/prompt_policies.py`. El dispatcher conserva compatibilidad de imports; las policies siguen persistidas en `main.prompt_policy_registry` sin cargar Markdown runtime.
- Verificacion focal SOA prompt policies: RED confirmado con `ImportError: cannot import name 'prompt_policies' from 'duckclaw.write_handlers'`; GREEN `uv run pytest tests/test_write_commands.py -q -k "prompt_policy_write_handlers_live_in_domain_module or prompt_policy_handler_upserts_updates_and_deactivates"` => `2 passed, 72 deselected`; lints de `write_handlers/prompt_policies.py`, `write_command_handlers.py` y `tests/test_write_commands.py` sin errores.
- Corte SOA `write_handlers.duckdb_maintenance` completado: el handler `drop_legacy_duckdb_objects` y sus helpers de sanitizacion de identificadores salieron de `packages/shared/src/duckclaw/write_command_handlers.py` hacia `packages/shared/src/duckclaw/write_handlers/duckdb_maintenance.py`. La operacion sigue transaccional por caller y no abre `BEGIN/COMMIT` propio.
- Verificacion focal SOA DuckDB maintenance: RED confirmado con `ImportError: cannot import name 'duckdb_maintenance' from 'duckclaw.write_handlers'`; GREEN `uv run pytest tests/test_write_commands.py -q -k "duckdb_maintenance_write_handlers_live_in_domain_module or drop_legacy_duckdb_objects_handler_drops_requested_objects"` => `2 passed, 73 deselected`; lints de `write_handlers/duckdb_maintenance.py`, `write_command_handlers.py` y `tests/test_write_commands.py` sin errores.
- Corte SOA `write_handlers.knowledge` completado: los handlers de `create_knowledge_source`, `upsert_knowledge_document`, `upsert_knowledge_chunks`, `deactivate_knowledge_source` y sus helpers de metadata/embedding salieron de `packages/shared/src/duckclaw/write_command_handlers.py` hacia `packages/shared/src/duckclaw/write_handlers/knowledge.py`. Con esto `write_command_handlers.py` queda como dispatcher/fachada y deja de ser god file de dominio.
- Verificacion focal SOA Knowledge/RAG: RED confirmado con `ImportError: cannot import name 'knowledge' from 'duckclaw.write_handlers'`; GREEN `uv run pytest tests/test_write_commands.py -q -k "knowledge_write_handlers_live_in_domain_module or knowledge_source_document_chunks_and_deactivate"` => `2 passed, 74 deselected`; lints de `write_handlers/knowledge.py`, `write_command_handlers.py` y `tests/test_write_commands.py` sin errores.
- Mini-audit `main.py`: sigue mezclando bootstrap/lifespan, root/health, history/Redis/SSE, auth Telegram Guard, chat invocation general, entrega outbound y montaje de routers, pero ya no posee raw write compat ni la excepcion RW de slash/fly.
- Corte slash/fly `/goals` + `/meditate` completado: ambos salieron de `LEGACY_RW_FLY_COMMANDS` y ahora abren `DuckClaw(read_only=True)` desde `services/api-gateway/core/fly_command_invocation.py`. La persistencia chat-scoped de `/meditate` ya delega en `set_chat_state_via_typed_command` / `UpsertAgentConfigEntriesCommand`, eliminando el uso raw legacy `enqueue_duckdb_write_sync` para ese flujo.
- Verificacion focal slash/fly `/goals` + `/meditate`: RED confirmado con `/goals` y `/meditate` aun presentes en `LEGACY_RW_FLY_COMMANDS` y `/meditate --delta` usando la ruta raw legacy; GREEN `uv run pytest tests/test_db_first_guardrails_static.py tests/test_meditate_delta_ticker.py -q -k "gateway_fly_rw_exception_excludes_read_only_safe_commands or execute_meditate_delta_with_read_only_handle_queues_typed_commands"` => `2 passed, 15 deselected`; verificacion ampliada `uv run pytest tests/test_meditate_delta_ticker.py tests/test_commands_crons_contract.py tests/test_db_first_guardrails_static.py -q` => `21 passed`; lints de `on_the_fly_commands.py`, `fly_command_invocation.py`, `test_meditate_delta_ticker.py` y `test_db_first_guardrails_static.py` sin errores.
- Corte slash/fly `/comfyui` completado: `/comfyui --provider local|fal` solo persiste el override chat-scoped `comfyui_provider`, por lo que ahora delega en `set_chat_state_via_typed_command` / `UpsertAgentConfigEntriesCommand` cuando recibe un handle read-only. `comfyui` salio de `LEGACY_RW_FLY_COMMANDS` y entra en `READ_ONLY_SAFE_FLY_COMMANDS`.
- Verificacion focal slash/fly `/comfyui`: RED confirmado con fallo por `INSERT INTO agent_config` desde handle read-only y con `comfyui` aun en la allowlist RW; GREEN `uv run pytest tests/test_comfyui_provider_command.py::test_comfyui_provider_with_read_only_handle_queues_typed_command 'tests/test_fly_command_invocation.py::test_db_first_fly_command_batch_is_read_only_safe[comfyui]' tests/test_db_first_guardrails_static.py::test_gateway_fly_rw_exception_excludes_read_only_safe_commands tests/test_db_first_guardrails_static.py::test_gateway_fly_rw_exception_lists_only_current_pending_commands -q` => `4 passed, 1 warning`.
- Corte slash/fly HITL completado: `/reject-code`, `/reject_code`, `/resolve-uncertainty` y `/resolve_uncertainty` salieron de `LEGACY_RW_FLY_COMMANDS` y ahora abren `DuckClaw(read_only=True)` desde el gateway. `reject-code` solo resuelve `tenant_id`/`last_requester_id` con lecturas de `agent_config` y delega el rechazo al servicio HITL existente; `resolve-uncertainty` delega al bridge epistémico con el mismo handle RO que ya usaba la ruta admin. No se agregaron comandos tipados nuevos porque este corte no encontro mutaciones chat-scoped ni task-audit propias en esos handlers.
- Verificacion focal slash/fly HITL: RED confirmado con fallos esperados por los cuatro aliases HITL en `LEGACY_RW_FLY_COMMANDS` y por fallback RW para comandos no listados. GREEN `uv run pytest tests/test_fly_command_invocation.py::test_db_first_fly_command_batch_is_read_only_safe tests/test_fly_command_invocation.py::test_db_first_fly_command_batch_opens_read_only_duckclaw tests/test_fly_command_invocation.py::test_unlisted_fly_command_defaults_to_read_only_duckclaw tests/test_db_first_guardrails_static.py::test_gateway_fly_rw_exception_excludes_read_only_safe_commands tests/test_db_first_guardrails_static.py::test_gateway_fly_rw_exception_lists_only_current_pending_commands -q` => `45 passed, 1 warning`.
- Cierre slash/fly RW completado: al quedar `LEGACY_RW_FLY_COMMANDS` vacio, se retiro `core/fly_command_invocation.py::_open_legacy_fly_duckclaw_rw` y el gateway ya no contiene `DuckClaw(... read_only=False)` para comandos slash/fly.

### Checklist De DB-First Total

- Ningun write estructurado del gateway abre `read_only=False`; excepciones temporales deben estar en allowlist con razon y fecha de retiro.
- Ningun router admin ejecuta `BEGIN TRANSACTION`; las transacciones viven en `services/db-writer` o en un runner de comandos tipados explicitamente testeado.
- Ningun mutator admin manda SQL raw a Redis; usa comandos Pydantic de `packages/shared/src/duckclaw/write_commands.py`.
- Todo mutator admin devuelve `task_id` o una lista `task_ids` cuando el trabajo se divide por documentos/chunks.
- `RawSqlCommand` queda solo para compatibilidad estrecha de `admin_sql`, con audit y sin usarse para flujos UI.
- Admin UI conserva BFF: browser -> `apps/duckclaw-admin/src/app/api/admin/*` -> gateway con `X-Admin-Key`; ningun secreto ni gateway URL directo en componentes.
- `admin_db_first.py` no recibe rutas nuevas; cada corte que salga agrega guardrail estatico para que no vuelva.
- No se hardcodean verticales ni dominios de negocio como defaults; todo comportamiento configurable entra por capabilities, prompt policies, runtime settings, worker catalog, grants o comandos tipados.

### Plan De Encarpetado Residual

- `services/api-gateway/main.py` debe seguir reduciendose hacia composition root: extraer primero slash/fly command handling y despues separar lifespan/bootstrap (`core/lifespan.py`) y health/root (`core/health.py`) con tests de contrato. No mezclar esos cortes con cambios de semantica de chat.
- `services/api-gateway/routers/admin.py` ya no debe recuperar mutators de templates ni DuckDB RW para auth. Siguiente corte admin recomendado: extraer overview/audit/remanentes pequenos a dominios propios y dejar `admin.py` como agregador/compat minima.
- `services/api-gateway/routers/admin_domains/playground_chat.py` sigue siendo un god file. La separacion incremental recomendada es `playground_llm_settings.py` para resolucion/escritura de LLM, `playground_vaults.py` para vault/conversation state, `playground_conversations.py` para historial y metadatos, y `playground_rag_context.py` para contexto de proyecto/RAG. No hacer este movimiento sin tests de rutas, model settings, vault override, streaming y conversaciones.
- Mapa SOA de handlers tipados: `packages/shared/src/duckclaw/write_handlers/` debe contener dominios pequenos y testeables (`runtime.py` para runtime settings y `agent_config`, `workers.py` para catalog/templates/skills/capabilities, `projects.py` para workspace/projects/managed draft, `auth.py` para console users/sesiones auth, `knowledge.py` para RAG/knowledge, `policies.py` para prompt policies, `access.py` para team access/shared grants y `maintenance.py` para DuckDB cleanup/raw compat estrecha). `write_command_handlers.py` debe quedar como dispatcher/composition root fino, importando handlers en top-level y sin abrir transacciones propias.
- Corte SOA iniciado: `packages/shared/src/duckclaw/write_handlers/runtime.py` es el owner de handlers runtime/chat-state/audit (`upsert_runtime_setting`, `upsert_agent_config_entries`, `delete_agent_config_entries`, `forget_chat_state`, `append_task_audit`). `packages/shared/src/duckclaw/write_handlers/access.py` es ahora el owner de handlers de access (`upsert_authorized_user`, `delete_authorized_user`, `upsert_shared_db_grant`, `delete_shared_db_grant`). `write_command_handlers.py` conserva exports legacy y dispatch, pero ya no contiene esas implementaciones. Guardrails focales: `tests/test_write_commands.py::TestCommandHandlers::test_runtime_write_handlers_live_in_domain_module` y `tests/test_write_commands.py::TestCommandHandlers::test_access_write_handlers_live_in_domain_module`.
- Mapa SOA de gateway admin: `admin.py` queda como agregador/compat; `admin_db_first.py` queda como deuda transicional que no debe crecer; `admin_domains/*` es destino por dominio. `playground_chat.py` debe partirse despues de la estabilidad DB-first en `playground_config.py`/`playground_model.py`, `playground_conversations.py`, `playground_invoke.py`, `playground_voice.py` y `playground_vaults.py`, con tests de rutas, model settings, vault override, streaming y conversaciones antes de mover codigo.

### Estrategia De Pruebas Del Refactor Admin

- Focal por corte: primero tests de `write_commands.py` y `write_command_handlers.py`; despues tests de router/BFF del dominio tocado; finalmente tests estaticos para asegurar que el codigo salio de `admin_db_first.py`.
- Regresion no-integration: ejecutar el subconjunto de admin/core afectado y, antes de cerrar un bloque grande, repetir la suite no-integration que ya dio `1350 passed, 2 skipped`.
- Integracion Redis/db-writer: para cada mutator migrado, levantar Redis + DB-writer y verificar que el `task_id` queda `success` en `admin_write_ledger` y que el estado se materializa en DuckDB.
- Smoke manual admin UI/playground: login, overview, templates, projects, policies, kanban, knowledge, playground con proyecto y model settings. Confirmar que todo pasa por BFF y que no hay errores 401/503 por headers o settings.

### Sensory TTS

- `integrations/sensory-node` ya no hardcodea voces Leila/Finanz/Quant Trader como defaults en el modelo Pydantic, el manifiesto versionado ni los scripts operativos principales de preparacion/regeneracion.
- `TTSRequest.voice_id` valida un slug generico; la aprobacion runtime sigue en el manifiesto cargado por `TTSEngine.has_voice`, no en una allowlist Python de dominios.
- `integrations/sensory-node/voices/manifest.json` queda intencionalmente vacio en core. Las voces se agregan por flujo offline administrado y no deben commitearse como defaults verticales.

### Meditate Harness

- `harness_core.states.meditate_state` y `harness_core.skills.emit_correction_delta` ya no usan fuentes stale de dominio como default. El default transversal para `PURGE_STALE_TASKS` es `main.task_audit_log`, alineado con `services/db-writer/models/meditate_state_delta.py`.
- `harness_core.skills.fetch_system_telemetry` trata `task_audit_log` como fuente DB-first de tareas stale usando `task_id` y `created_at` cuando no existe `updated_at`, sin crear migraciones nuevas ni rutas write directas.
- `tests/test_forge_legacy_cleanup.py::test_meditate_harness_uses_transversal_stale_task_source_table` protege que el harness de meditate no vuelva a depender de tablas Quant como default.

## Patrones Nuevos A Usar

### Modulos Canonicos

- `duckclaw.manager.*`: ownership de manager graph, routing, planning, invocation, fast plans, fast replies, task classification y formatting.
- `duckclaw.commands.*`: ownership de comandos chat/team/whitelist antes incrustados en `graphs/on_the_fly_commands.py`.
- `duckclaw.homeostasis.*`: ownership de helpers transversales de homeostasis/goals alignment que no pertenecen a legacy Forge.
- `duckclaw.worker_quality_signals`: owner DB-first de señales de calidad administrables por worker/tenant; evita reabrir manifests/filesystem como registry implícito de goals.
- `duckclaw.egress.evidence_validator`: validacion transversal de evidencia y citas.
- `duckclaw.egress.tool_response_repair`: reparacion transversal de respuestas crudas de tools.
- `duckclaw.db_write_queue`, `duckclaw.write_commands` y `duckclaw.write_command_handlers`: cola singleton, confirmacion de task status y contrato de mutacion DB-first.
- `duckclaw.shared_db_grants`: grants compartidos como control plane, no como bypass de filesystem.
- `duckclaw.workers.discovery`: discovery/listado DB-first de workers, con filesystem limitado a `default` y fachada legacy desde `duckclaw.workers.factory`.
- `duckclaw.workers.visual_evidence_policy`: politicas puras de retry visual del grafo de worker, sin defaults de dominio ni fallback filesystem.
- `duckclaw.workers.tool_output_truncation`: truncado y compactacion pura de salidas de tools antes de alimentar contexto LLM; no decide provider budgets ni routing de tools.
- `duckclaw.workers.provider_input_budget`: presupuesto de entrada por proveedor y poda pura de historial, apoyado en `tool_output_truncation`; lee runtime settings DB-first de provider budget y no decide routing ni tool binding.
- `duckclaw.workers.context_monitor`: compresion generica de contexto y nodo LangGraph derivado de `context_pruning_config`; no debe contener prompts ni compuertas por worker vertical.
- `duckclaw.workers.tool_binding`: filtros puros de superficie de tools, helpers `tool_choice` y deteccion de tool calls para binding LLM; no registra bridges, no resuelve workers y no decide policies DB-first.
- `duckclaw.runtime_session_settings`: flags runtime chat-scoped en `admin_runtime_settings` (`runtime.session.*`), con `commands.runtime_toggles` y `commands.model_setup` como owners de writes desde chat/admin.

### Regla Para Runtime Policies

Las runtime policies se agregan como filas/capabilities/policies DB-first. No se agregan como defaults de dominio en Python. Si una policy necesita keywords, regex, prompt, tools o permisos, se versiona en DB y se prueba con migraciones/fixtures.

### Regla Para Agentes De Filesystem

El unico worker/agente que puede existir como layout versionado en filesystem es `default`. Cualquier otro `worker_id` debe resolverse desde `admin_worker_catalog`/catalogo DB-first o desde una extension externa, no desde carpetas `templates/workers`, `forge/seed`, manifests verticales ni alias de layout. Si no hay fila de catalogo para un agente no-default, el runtime debe fallar en vez de hacer fallback a filesystem.

### Regla Para Compatibilidad Legacy

Una fachada legacy es aceptable solo si:

- Delegar al owner canonico es trivial y testeado.
- No reintroduce conocimiento vertical.
- Tiene una razon temporal clara.
- No se convierte en el nuevo punto de extension.

## Guardrails Y Tests Relevantes

Ejecutar o actualizar estos tests cuando un corte toque el area correspondiente:

- `tests/test_manager_core_vertical_guardrail.py`: prohibe verticales en `packages/agents/src/duckclaw/manager/graph.py`, `routing.py` y `fast_plans.py`.
- `tests/test_manager_task_classification.py`: protege clasificacion generica y evita special-cases de Job/Finance/Quant/PQRS/Leila/War Room.
- `tests/test_manager_fast_plans.py`: valida fast replies y fast plans DB-first via prompt/capability policies.
- `tests/test_manager_worker_reply_formatting.py`: asegura que formatting de respuestas de subagentes viva en `duckclaw.manager.worker_reply_formatting`.
- `tests/test_forge_legacy_cleanup.py`: protege removal de paquetes Forge legacy, shims `WORKER_*`, env vars DB path de dominio, War Room core, CREATE TABLE fuera de allowlists y read-write DuckDB fuera de allowlists.
- `tests/test_package_reorg_contracts.py`: protege facades publicas compartidas y ownership de `duckclaw.manager.graph`.
- `tests/test_worker_factory_modular_boundaries.py`: protege que `template_registry`, `load_manifest` y `workers.discovery.list_workers` expongan desde filesystem solo `default`; `workers.factory.list_workers` queda como fachada compatible y agentes extra deben venir de DB/catalogo.
- `tests/test_worker_factory_modular_boundaries.py::test_tool_output_truncation_owns_helpers_with_factory_facade` y `tests/test_tool_output_truncation.py`: protegen que el truncado de `ToolMessage`, sandbox outputs y sanitizacion de tools `reddit_*` vivan en `duckclaw.workers.tool_output_truncation`, con fachada legacy desde `workers.factory`.
- `tests/test_worker_factory_modular_boundaries.py::test_provider_input_budget_owns_helpers_with_factory_facade` y `tests/test_provider_input_budget.py`: protegen que presupuesto de entrada por proveedor, estimacion de tokens, runtime settings DB-first de Groq/MLX y poda pura de historial vivan en `duckclaw.workers.provider_input_budget`, con fachada legacy desde `workers.factory`.
- `tests/test_worker_factory_modular_boundaries.py::test_context_monitor_owns_summary_helpers_with_factory_facade` y `tests/test_context_monitor.py`: protegen que resumen/compresion de contexto y el builder del nodo `context_monitor` vivan en `duckclaw.workers.context_monitor`, con fachada legacy desde `workers.factory` y sin hardcodes BI en la policy de compresion.
- `tests/test_worker_factory_modular_boundaries.py::test_tool_binding_owns_tool_surface_helpers_with_factory_facade` y `tests/test_worker_tool_binding.py`: protegen que filtros puros de binding, helpers `tool_choice` y deteccion de tool calls vivan en `duckclaw.workers.tool_binding`, con fachada legacy desde `workers.factory`.
- `tests/test_graph_tool_catalog.py`: protege que `general_graph.py` use `duckclaw.graphs.tool_catalog` para defaults runtime, que el fallback legacy no active escritura SQL ni sandbox sin policy DB/caller, y que `chat_heartbeat.py` no cargue mensajes de tools desde Markdown ni conserve ejemplos verticales.
- `tests/test_chat_heartbeat.py`: protege que el estado `/heartbeat on|off` sea DB-first sin requerir Redis, que DB gane sobre claves Redis legacy, que callers runtime sin handle usen el provider DB configurado, y que Redis siga disponible solo como fallback legacy/event bus.
- `tests/test_read_pool.py`: protege que `workers.read_pool` no reintroduzca hardcodes BI y que los bloqueos de `SELECT *` sin `LIMIT` dependan de capability DB-first generica, no de ids de worker.
- `tests/test_worker_factory_modular_boundaries.py::test_core_admin_runtime_does_not_hardcode_non_default_platform_worker`: prohibe que admin catalog/runtime/router vuelvan a fijar `platform-orchestrator` como worker no-default de sistema.
- `tests/test_admin_workspace_catalog.py::test_orchestrator_draft_uses_db_first_prompt_policy_for_prompt_and_naming`: protege compatibilidad de la ruta legacy y que el flujo administrado use la policy DB-first para prompt y naming.
- `tests/test_admin_projects_ui_static.py::test_managed_workspace_draft_copy_and_symbols_avoid_orchestrator_product_naming`: prohibe reintroducir "Platform Orchestrator", "orchestrator wizard" o helpers `Orchestrator*` como naming interno/copy fuera del alias `/workspace/orchestrator/*` y topologias genericas.
- `tests/test_prompt_policies.py::test_managed_workspace_draft_policy_is_seeded_by_migrations`: protege que la policy generica del flujo administrado exista como fila de migracion, no como Markdown/runtime Python.
- `tests/test_commands_chat_state_contract.py`: protege ownership de `duckclaw.commands.chat_state`.
- `tests/test_commands_team_templates_contract.py`: protege ownership de `duckclaw.commands.team_templates`.
- `tests/test_commands_team_access_contract.py`: protege ownership de whitelist, comandos typed DB-writer y ausencia de War Room en team access.
- `tests/test_commands_vaults_contract.py`: protege ownership de `/vault` y evita que sus helpers vuelvan a `graphs/on_the_fly_commands.py`.
- `tests/test_commands_crons_contract.py`: protege ownership de `/crons`, helpers de schedule proactivo y compatibilidad de imports legacy desde `graphs/on_the_fly_commands.py`.
- `tests/test_commands_goals_contract.py`: protege ownership de `/goals`, helpers del manifiesto homeostasis, compatibilidad de imports legacy desde `graphs/on_the_fly_commands.py` y que el registry venga de `duckclaw.worker_quality_signals`, no de manifests/filesystem.
- `tests/test_worker_quality_signals.py`: protege roundtrip DB-first de señales de calidad y su traducción interna a `BeliefRegistry` para `/goals`.
- `tests/test_commands_sensors_contract.py`: protege ownership de `/sensors`, compatibilidad de imports legacy desde `graphs/on_the_fly_commands.py` y ausencia de defaults verticales en el modulo canonico.
- `tests/test_commands_audit_contract.py`: protege ownership de `/audit` y `save_last_audit`, compatibilidad de imports legacy desde `graphs/on_the_fly_commands.py`, ausencia de defaults verticales y ausencia de conexiones DuckDB RW directas en el modulo canonico.
- `tests/test_commands_history_contract.py`: protege ownership de `/history`, `append_task_audit`, helpers de `task_audit_log`, compatibilidad de imports legacy desde `graphs/on_the_fly_commands.py`, ausencia de defaults verticales y uso de comando tipado para la cola DB-writer.
- `tests/test_commands_model_setup_contract.py`: protege ownership de `/model`, `/models`, `/setup`, `/prompt`, resolvers LLM y `get_effective_system_prompt`, compatibilidad de imports legacy desde `graphs/on_the_fly_commands.py`, ausencia de defaults verticales y ausencia de fallback a workers por filesystem en el modulo canonico.
- `tests/test_commands_health_contract.py`: protege ownership de `/health` y `/heartbeat`, compatibilidad de imports legacy desde `graphs/on_the_fly_commands.py`, ausencia de defaults verticales, ausencia de writes DuckDB y ausencia de imports desde `duckclaw.graphs` en el owner canonico.
- `tests/test_commands_runtime_toggles_contract.py`: protege ownership de `/sandbox`, `/internet`, aliases `/red`/`/network` en dispatcher, compatibilidad de imports legacy desde `graphs/on_the_fly_commands.py`, ausencia de defaults verticales, ausencia de imports desde `duckclaw.graphs` en el owner canonico y uso de callback explicito para cleanup sandbox.
- `tests/test_sandbox_network_policy.py`: protege que la policy efectiva de red por chat lea `runtime.session.sandbox_network_enabled` desde `admin_runtime_settings`, no desde `agent_config` ni desde `graphs.on_the_fly_commands`.
- `tests/test_api_gateway.py` y `tests/test_forge_legacy_cleanup.py::test_team_env_does_not_infer_tenant_from_vertical_process_or_path_names`: protegen que el tenant default del gateway no vuelva a inferirse desde PM2/rutas ni desde nombres verticales en `team_env.py`.
- `tests/test_telegram_compact_webhook_routes.py::test_parse_legacy_path_uses_db_first_default_tenant` y `tests/test_multi_vault_system.py::test_vault_default_tenant_label_uses_db_first_runtime_setting`: protegen que Telegram compact legacy y `/vault` usen el owner DB-first de tenant default.
- `tests/test_tool_response_repair.py`: protege repair transversal y ausencia de marcadores verticales en `tool_response_repair.py`.
- `tests/test_answer_evidence_validator.py`: protege auditoria transversal de evidencia/citas.
- `tests/test_write_commands.py`: protege serializacion y handlers de comandos tipados.
- `tests/test_admin_router.py`: cubre contrato de Playground, incluyendo estado de voz.
- `tests/test_admin_router_split_static.py`: protege que rutas extraidas de admin (`kanban`, `kanban_runtime`, `prompt_policies`) vivan en `admin_domains/*` y no regresen a `admin_db_first.py`.
- `tests/test_admin_projects_ui_static.py`: protege contrato BFF/UI de proyectos, managed workspace draft, RAG, prompt policies y naming sin Platform Orchestrator.
- `integrations/sensory-node/tests/test_models.py`: protege que Sensory no vuelva a hardcodear voces Leila/Finanz/Quant Trader como defaults versionados y que `voice_id` sea manifiesto-owned en vez de una allowlist Python vertical.
- `tests/test_forge_legacy_cleanup.py::test_labor_vertical_residues_are_absent_from_core_config_and_telegram_tests`: prohibe residuos Job Hunter/laborales en `on_the_fly_commands.py`, `sandbox.py`, `.env.example`, `config/` y tests Telegram/PM2 que deben usar workers genericos.
- `tests/test_forge_legacy_cleanup.py::test_meditate_harness_uses_transversal_stale_task_source_table`: prohibe que `meditate_state.py` o `emit_correction_delta.py` vuelvan a usar fuentes stale Quant como default.
- `tests/test_forge_legacy_cleanup.py::test_homeostasis_operation_docs_use_generic_metrics`: prohibe que las docs operativas de Homeostasis usen ejemplos financieros/verticales como runtime positivo.

Comandos sugeridos por corte:

```bash
uv run pytest tests/test_manager_core_vertical_guardrail.py tests/test_manager_task_classification.py tests/test_forge_legacy_cleanup.py -q
```

```bash
uv run pytest tests/test_package_reorg_contracts.py tests/test_commands_chat_state_contract.py tests/test_commands_team_templates_contract.py tests/test_commands_team_access_contract.py -q
```

```bash
uv run pytest tests/test_tool_response_repair.py tests/test_answer_evidence_validator.py tests/test_manager_fast_plans.py tests/test_write_commands.py -q
```

## Allowlists Y Residuos Conocidos

Estos residuos existen en el repo actual y no deben confundirse con patrones a copiar:

- `packages/agents/src/duckclaw/workers/factory.py`: sigue siendo una superficie grande de ensamblaje de tools/runtime. Los cortes recientes retiraron orquestacion determinista Quant/IBKR/Finance y Job Hunter/GitHub hardcodeado; el pendiente ya no debe resolverse con ramas por dominio, sino con capabilities/tools DB-first o extensiones.
- `packages/agents/src/duckclaw/graphs/on_the_fly_commands.py`: ya salio de la allowlist de residuos verticales del core command graph y delega `/vault`, `/crons`, `/goals`, `/sensors`, `/audit`, `/history`, `/model`, `/models`, `/setup`, `/prompt`, `/health`, `/heartbeat`, `/sandbox` y `/internet`. Sigue siendo una fachada grande con ownership residual de comandos genericos; el pendiente es seguir extrayendo otros helpers transversales hacia `duckclaw.commands.*`, no reintroducir dominios.
- `packages/agents/src/duckclaw/graphs/sandbox.py`: el prompt y summary del browser sandbox ya son genericos respecto a busqueda laboral; los pendientes restantes son cleanup de ejemplos/heuristicas de navegacion no laborales.
- `packages/agents/src/duckclaw/finance/*` y `packages/agents/src/duckclaw/quant/*`: paquetes verticales pendientes de remover o sacar del core despues del corte de factory.
- `services/db-writer/quant_state_delta_handler.py` y `services/db-writer/models/quant_state_delta.py`: handlers/DTOs verticales residuales; el loop no esta activo en `services/db-writer/main.py`, pero los archivos siguen presentes.
- `services/api-gateway/routers/admin.py`: ya no conserva mutators de templates con DuckDB RW directo ni RW allowlisted para auth login, pero sigue siendo god file por overview/audit/ops/helpers de compat. `services/api-gateway/routers/admin_domains/visual_assets.py` sigue en allowlist por diagnosticos/admin context pendientes de genericizar.
- `services/api-gateway/routers/admin_domains/access_management.py`: los mutadores admin de access management delegan en comandos tipados/DB-writer y el modulo salio de la allowlist de conexiones DuckDB read-write directas.
- `scripts/deployment/patch_tts_production_env.py`, `scripts/deployment/test_sensory_tts.py`, `scripts/deployment/test_tts_duration_remote.py`, `scripts/deployment/debug_tts_mac.py` e `integrations/sensory-node/scripts/check_tts_amplitude.py`: smoke/debug/patch scripts de TTS aun nombran voces legacy. No deben copiarse como defaults; el siguiente corte Sensory debe moverlos a ids genericos o resolver voz desde `DUCKCLAW_TTS_VOICE_MAP`/manifest.
- `packages/duckops/duckops/sovereign/materialize.py`: contiene comentarios/operaciones con nombres legacy; no es runtime manager core.

Las allowlists vivas estan declaradas en `tests/test_forge_legacy_cleanup.py`. Si un futuro subagente agrega una excepcion, debe agregar tambien una razon explicita y preferiblemente una tarea de follow-up para retirarla.

## Instrucciones Para Subagentes

1. No buscar ni depender de `SDD_INDEX.md`. Este documento es la fuente de contexto para el refactor DB-first/core cleanup.
2. Leer tambien las specs puntuales relacionadas antes de tocar codigo, por ejemplo `docs/specs/features/platform/RAG_TRANSVERSAL_DB_FIRST.md`, `ADMIN_RUNTIME_SETTINGS.md`, `ADMIN_PROJECT_DETAIL_AND_PLAYGROUND_FIXES.md`, `ADMIN_IDENTITY_RBAC_ERD.md` o la spec del area tocada.
3. Trabajar en TDD RED/GREEN para cambios de comportamiento. Primero agrega o ajusta el test guardrail/contrato, verifica que falla por la razon esperada, luego implementa.
4. No agregar defaults de dominio en Python. Si necesitas comportamiento configurable, usa DB-first: capabilities, prompt policies, runtime settings, worker catalog, grants o comandos tipados.
5. No mover verticales al core con otro nombre. Si una funcion contiene conocimiento de Quant, Finance/IBKR, PQRSD, Leila, War Room o Job Hunter, debe vivir como extension vertical o estar alimentada por DB.
6. Antes de tocar `workers/factory.py` u `on_the_fly_commands.py`, identifica la menor extraccion canonica posible y agrega un test de ownership como los existentes.
7. Antes de tocar DuckDB writes, revisa las allowlists en `tests/test_forge_legacy_cleanup.py` y prefiere `duckclaw.write_commands` + DB-writer.
8. Antes de tocar egress, revisa `duckclaw.egress.evidence_validator`, `duckclaw.egress.tool_response_repair` y sus tests. No crear otro repair por vertical.
9. Al terminar, ejecuta el subconjunto de tests del area y al menos el guardrail core/vertical si tocaste manager, runtime, gateway o comandos.

## Pendientes Recomendados

### Inmediato

- Ejecutar el siguiente corte prioritario de DB-first total sobre slash/fly: retirar `_open_legacy_fly_duckclaw_rw` y la allowlist gateway de `DuckClaw(read_only=False)` si no queda ningun consumidor externo que requiera esa compatibilidad auditada.
- En cada corte, escribir primero el contrato de comando tipado y handler en `packages/shared/src/duckclaw/write_commands.py` y `write_command_handlers.py`; despues mover la ruta a `services/api-gateway/routers/admin_domains/*` y dejar guardrail estatico.
- Reducir el resto de `on_the_fly_commands.py` solo cuando el corte tenga comando tipado equivalente y el gateway pueda invocarlo sin DuckDB RW directo.
- Mantener `workers/factory.py` como superficie de alto riesgo: tras extraer discovery/listado, retry visual, truncado de tool outputs, provider input budget, context monitor, helpers puros de tool binding/tool_choice y flags sandbox DB-first, los siguientes cortes deben migrar registro de tools/capabilities y runtime assembly hacia owners pequenos DB-first o extensiones, no a nuevas ramas por dominio.

### Mediano

- Crear un owner DB-first tipado para el registry administrable de beliefs si `/goals` necesita autocompletar metas por worker; no reabrir manifests de workers no-default como fallback.
- Sacar `packages/agents/src/duckclaw/finance/*` y `packages/agents/src/duckclaw/quant/*` hacia extensiones verticales o policies DB-first fuera del core.
- Remover `services/db-writer/quant_state_delta_handler.py` y `services/db-writer/models/quant_state_delta.py` cuando exista migracion/archivo de compatibilidad claro; el loop ya no esta activo, pero los archivos siguen siendo residuo vertical.
- Seguir retirando allowlists de routers admin vecinos (`admin.py`, `admin_db_first.py`, `admin_domains/visual_assets.py`, `runtime_config.py`, `duckdb_explorer.py` y flujos relacionados) cuando cada mutacion tenga comando tipado, BFF claro y guardrail focal.
- Revisar imports legacy restantes de harness/homeostasis y `graphs/tools.py` en cortes dedicados; los owners canonicos ya existen, pero aun quedan fachadas e imports historicos.

### Opcional O Bloqueado Por Input Admin

- Genericizar scripts Sensory legacy (`patch_tts_production_env.py`, `test_sensory_tts.py`, `test_tts_duration_remote.py`, `debug_tts_mac.py`, `check_tts_amplitude.py`) para resolver voces desde manifest/env administrado, o moverlos fuera del core si siguen siendo smoke scripts de dominio.
- Extender UI/admin runtime settings para exponer `gateway.default_tenant_id` y otros defaults transversales solo si el admin necesita gestionarlos desde pantalla; el contrato DB-first ya existe.
- Extender el barrido de residuos laborales y verticales fuera del runtime core separando menciones negativas de specs/tests contra ejemplos runtime.
- Mantener tests guardrail cerca del contrato. Cada cleanup que cierre un residuo debe retirar o estrechar su allowlist.
