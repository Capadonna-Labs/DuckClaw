-------- PARA LEVANTAR EL SISTEMA --------

./duckops-up.sh                            # macOS/Linux/WSL — instala uv si falta
.\duckops-up.ps1                           # Windows PowerShell — instala uv si falta

uv run duckops up                          # si ya tienes uv: prereqs + migrate + PM2 + admin
uv run duckops doctor --strict
uv run duckops configure                   # primera vez o cambio de .env

uv run duckops down --all
uv run duckclaw-migrate                    # tras down si hay lock DuckDB
uv run duckops up

--------- INTERFAZ GRAFICA ----------
cd apps/duckclaw-admin
pnpm install
pnpm dev
