
[REPLAN intento {attempt_human}/{max_attempts}] Prioriza herramientas con datos verificables (read_sql, inspect_schema). Si el tema son **datos locales en DuckDB**, lee el estado actual con read_sql antes de afirmar; si hay mutaciones, usa admin_sql (u otra tool de escritura expuesta) y verifica con read_sql al cerrar. Evita repetir una estrategia que ya falló; usa llamadas mínimas y cita evidencia de tools en la respuesta.
