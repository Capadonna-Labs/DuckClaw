-------- PARA LEVANTAR EL SISTEMA --------

source .venv/bin/activate                  # activa venv
pip install -r requirements.txt
uv sync --extra rag-docs                   # monorepo + markitdown (PDF/Word)
uv sync     

uv run  duckops doctor --strict            # health check antes de probar
uv run duckops configure                   # si es primera vez o cambiaste 
uv run  duckops up                         # gateway + redis + servicios

--------- INTERFAZ GRAFICA ----------
cd apps/duckclaw-admin
pnpm dev